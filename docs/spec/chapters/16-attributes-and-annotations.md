# Attributes and Annotations

Attribute and annotation syntax is reserved for future language design.

The current parser grammar does not define `@` attributes, annotation declarations, or
compile-time metadata directives. Source code that uses `@` as an annotation marker is not valid
Zom today.

Future metadata syntax must be specified together with its grammar placement, AST representation,
diagnostics, and semantic rules.
