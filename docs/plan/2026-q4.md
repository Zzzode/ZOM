# ZOM Compiler Plan - 2026 Q4 (October - December)

> Planning window: **2026-10-01 to 2026-12-31**.
> Authored: 2026-08-25 (during Q3). Owner: compiler team.
> Scope: one quarter. Prior and subsequent quarters live in sibling files under
> `docs/plan/` (e.g. `2027-q1.md`). This document is the committed plan for Q4
> only; revise it in place during the quarter and archive it unchanged after.
>
> This plan is anchored to two hard constraints: (1) the RFC governance
> dependency chain (a status transition must precede any implementation it
> gates), and (2) on-disk technical reality verified against the source tree as
> of 2026-08-25, not aspiration. Every item is classified **reachable now**,
> **spec-blocked**, or **governance-blocked** so no work begins that a
> prerequisite forbids.

## Entry state (as of 2026-08-25, verified)

The frontend pipeline runs end-to-end:

```
Lexer -> Parser -> Binder -> Checker -> CheckedModule -> HIR -> Built MIR
```

- Built MIR emits `Return`, `Unreachable`, `Call`, `Goto` (0x04), `SwitchInt`
  (0x05) terminators and a `Comparison` (0x03) rvalue.
- Lowered shapes today: scalar local writes/transfers, nominal aggregate init
  and field writes, same-module direct calls with scalar-literal or parameter
  arguments, four-block diamond conditionals (primitive relational comparisons
  `Eq/Ne/Lt/Le/Gt/Ge` over two scalar operands, each a parameter or scalar
  literal), returned comparison results, and a minimal reducible `while` loop.
- Ownership rail (RFC 0007/0013) runs fail-closed; general region liveness,
  escape/capture production, and verified typestate remain in progress.
- **No backend:** no target LIR, ABI lowering, LLVM IR, object emission, or
  linking. ZOM cannot yet produce a native binary.

## RFC status at plan start (verified on disk 2026-08-25)

| RFC | Title | Status |
|---|---|---|
| 0006 | Error lowering runtime ABI | IMPLEMENTING |
| 0007 | Borrow/lifetime/ownership checker | IMPLEMENTING |
| 0009 | Call dispatch and operator lowering | IMPLEMENTING |
| 0010 | Intermediate representation pipeline | IMPLEMENTING |
| 0016 | Context-bound target registry verification | REVIEW |
| 0021 | Target-aware LIR and LLVM translation | DRAFT |
| 0022 | Flow-sensitive type refinement and null safety | REVIEW |
| 0043 | Platform link and executable publication | DRAFT |
| 0046 | Forced error operator panic-abort ABI | DRAFT |

## Dependency chain (the spine)

```
RFC 0016 (REVIEW) --> RFC 0021 (DRAFT) --> LIR/LLVM backend --> native binary
                                     \--> RFC 0043 (link + executable)
RFC 0006 error-union ABI descriptor --> RFC 0046 (panic ABI) --> error-operator lowering
(semantic error-union shape already exists as RFC 0005 ErrorUnionShapeFact)
```

---

## Q4 OKRs (framing added 2026-08-26)

The G-numbered goals below are the concrete, per-commit work items and their
history. This section groups them into six Objectives with measurable Key
Results, so the quarter reads as outcomes rather than a task list. Every KR
carries an honest reachability class; a KR that a governance transition or the
environment forbids is written as **conditional** and is never reported "done"
by admitting a substitute (e.g. ambient LLVM). RFC statuses in this section are
the live on-disk values as of 2026-08-26 (0016 ACCEPTED, 0021 ACCEPTED, 0022
REVIEW, 0043 DRAFT), not the plan-start snapshot table above.

### Pre-quarter rebaseline (2026-08-30)

Q4 has not started: the planning window opens on 2026-10-01. Implementation
advanced materially during the Q3 planning period, so completion is now graded
against the exact KR deliverable rather than the earlier, narrower status notes.
The status vocabulary is:

- **Complete** - the exact KR deliverable is committed, has a production
  consumer where the KR requires one, and has project-native verification.
- **Candidate** - the implementation exists and focused verification passes in
  the current worktree, but the change is not yet committed and integrated.
- **Partial** - a prerequisite or foundation landed, but the named user-facing
  or production-boundary deliverable is absent.
- **Blocked** - no sound non-dead slice is currently available; the blocking
  dependency is recorded and no placeholder is admitted.

There are 27 KRs across O1-O6. On this strict basis, 19 are complete, seven are
partial or candidate, and one is blocked. Landing the three current worktree
candidates (KR4.4, KR5.3, and KR5.4) would raise the committed count to 22; a
passing remote backend CI run would then close KR2.5 and raise it to 23. A
governance transition does not complete a KR that also names an implementation
slice, and an underlying capability does not complete a KR that explicitly names
a missing CLI command.

### O1 - Lower a meaningful language subset reliably to Built MIR
*Fully reachable now. This is the quarter's delivery center of gravity: it is
the only Objective that ships purely by writing and verifying code.*

- **KR1.1** G6a-2: a mutable-local write RHS may be a primitive binary
  operation. Requires generalizing the fixed 2-node-ids-per-write HIR stride.
- **KR1.2** G6b: a non-empty `while` body that mutates locals lowers to a
  reducible CFG. Requires redesigning `HirLoopStatement` to carry body
  statements.
- **KR1.3** RFC 0009 call-dispatch widening: at least one of multi-argument
  direct calls or method-receiver calls lowers end-to-end to Built MIR.
- **KR1.4** Publish a "constructs that lower end-to-end" inventory, each backed
  by a conformance corpus and fixed-id verifier coverage; report the count at
  quarter close against the entry-state baseline (measurable growth, not a
  vibe).

### O2 - Clear the two backend gates and reach first native output (AGGRESSIVE)
*Partially reachable; heavily environment-dependent. Chosen aggressive on
2026-08-26: pursue the first end-to-end MIR -> LIR -> LLVM path. The aggressive
KRs (KR2.3, KR2.4) are **conditional on KR2.1 clearing**; if the environment
cannot provide LLVM 22.1.8 they are blocked and will be reported blocked, not
worked around.*

- **KR2.1** G8: align the toolchain to LLVM `22.1.8` (from `19.1.5`) through the
  RFC 0016-accepted CMake/CI transaction. **May require environment
  provisioning outside this repo; ambient `19.1.5` must not be admitted.** This
  KR gates KR2.2-KR2.4. **Assessment 2026-08-27:** the pin is valid, not wishful
  - `llvmorg-22.1.8` is a real released upstream tag (object
  `e013073558445169e8732e25fa86e9913bfdd24e`). The environment provides only
  Homebrew LLVM `19.1.5`; no `22.1.8` is installed and (uid 1001, no root,
  network-restricted `brew`) it cannot be provisioned from within this session.
  KR2.1 therefore splits:
    - **KR2.1a (re-assessed 2026-08-27: NOT cleanly landable now):** the intent
      was an in-repo fail-closed CMake gate. Reading the accepted RFC 0016 CMake
      contract (that RFC's "LLVM build and CI contract") shows the gate is an
      **unconditional top-level configure gate** requiring exact `22.1.8`, whose
      positive fixture records a real resolved `llvm-config --version == 22.1.8`.
      Three facts make it non-landable here: (i) the frontend links no LLVM today
      (no `find_package(LLVM)` in any CMakeLists, no target links it), so wiring a
      mandatory gate in a `19.1.5`-only env breaks the frontend-only build used to
      verify every slice; (ii) the positive fixture cannot pass without a real
      `22.1.8`; (iii) it is RFC 0016 IMPLEMENTING-phase work, but 0016 is
      ACCEPTED-not-IMPLEMENTING and the IMPLEMENTING pointer is itself gated on
      `22.1.8`. An "opt-in/isolated" variant would either deviate from the
      accepted contract (drift) or be dead code until `22.1.8` (design principle
      #4). Conclusion: KR2.1a collapses into KR2.1b - it is blocked on the same
      `22.1.8` provisioning, not separately reachable.
    - **KR2.1b (DONE 2026-08-27 via from-source build).** The apt.llvm.org
      prebuilt hit a hard glibc floor (built for glibc 2.38+, host is Debian
      glibc 2.36: `llc` would not run, links failed on `__isoc23_*`), and the
      glibc-2.36-compatible GitHub tarball was network-unreachable. Resolved by
      building LLVM 22.1.8 from source ON this host, which is glibc-2.36-native
      by construction. A fresh isolated shallow clone of `llvmorg-22.1.8` (commit
      `ca7933e47d3a`) was configured X86+AArch64 Release with all optional deps
      off and built with `ninja -j52` (56-core host), installed to
      `$HOME/toolchains/llvm22`. Verified end-to-end: `llvm-config --version` =
      `22.1.8`; targets are exactly `X86 AArch64`; `llc --version` RUNS (rc=0,
      the exact tool that failed on the prebuilt); all 11 RFC-0016 components
      resolve; and a `verifyModule` program compiles, links, and runs (rc=0,
      system-libs reduced to `-lrt -ldl -lm`). `LLVM_DIR` =
      `$HOME/toolchains/llvm22/lib/cmake/llvm`. The earlier Swift `llvm-project`
      checkout was NOT touched (it is a dirty third-party tree). This toolchain
      is a local dev provision; CI still uses the RFC 0016 ubuntu-24.04/macos-15
      contract. With a runnable 22.1.8 present, KR2.1a and KR2.2-KR2.4 are now
      unblocked.
- **KR2.2** *(DONE 2026-08-27)* RFC 0016 -> IMPLEMENTING (`cb8e1e3b`, first slice
  landed as evidence) and RFC 0021 -> IMPLEMENTING (`f9c670bf`, implementation
  pointer set; both preconditions - RFC 0016 IMPLEMENTING and the provisioned
  22.1.8 toolchain - cleared). Real transitions with landed evidence; nothing
  fabricated.
- **KR2.3** *(DONE 2026-08-27, `fabf520d`)* Landed the first authorized RFC 0021
  slice: the LIR identity / carrier / layout / ABI / runtime-symbol /
  source-location / revision codec foundation (pure data, no live MIR consumer,
  no LLVM linkage). The live LirAlgebraCodec reproduces the RFC's documented
  56-byte empty-registry oracle + SHA-256 `03106c34...`; oracle, mutation, and
  fail-closed tests pass under the sanitizer build.
- **KR2.4** *(DONE 2026-08-27, `4f078d78`)* A scalar module initializer lowers
  MIR -> LIR -> LLVM IR and passes `llvm::verifyModule`, emitting
  `define i32 @zom.module_init() { entry: ret i32 42 }`. A minimal LIR model +
  `MirToLirLowering::lowerScalarInitializer` (fails closed outside the shape) feed
  a new `compiler/backend/llvm/` Pimpl shim - the sole TU including `llvm/...` -
  which builds the module in RFC 0021 deterministic order and publishes only on a
  clean verifier. Gated behind `ZOM_ENABLE_LLVM_BACKEND` (OFF = frontend
  unaffected, no LLVM linked). Object emission and linking were outside KR2.4
  and were later promoted into O4 and O5.
- **KR2.5** *(added 2026-08-27; reachable now; strategy B - vendored self-managed)*
  Make LLVM 22.1.8 a *stable, reproducible, self-managed* dependency, following
  the Rust/Swift model for a serious self-hosted-IR-to-LLVM compiler rather than
  the lighter system-package model of out-of-tree tools. The exact-version pin +
  RFC 0016 fail-closed gate remain the reproducibility backbone; strategy B adds
  repository ownership of the exact source:
    - **Lock the exact LLVM source**, not just a version number: record
      `llvmorg-22.1.8` and its commit `ca7933e47d3a` in a repository lock (a
      pinned submodule or an explicit lock file), so the dependency is
      reproducible to the commit (Rust submodule / Swift monorepo discipline).
      This also leaves room to carry ZOM-specific LLVM patches later, which the
      system-package route forecloses.
    - **Dev provisioning**: a `scripts/` helper that builds the locked tag from
      source into a local prefix (e.g. `$HOME/toolchains/llvm22`), fixing
      `LLVM_DIR`. Building from source is required here anyway on heterogeneous
      glibc hosts (this host is Debian glibc 2.36; the apt.llvm.org prebuilt is
      glibc 2.38 and will not run), which is itself an argument for self-managed
      source rather than system packages.
    - **CI**: fixed `ubuntu-24.04` / `macos-15` runners; prefer a *cached
      prebuilt of the locked commit* (rustc `download-ci-llvm` style) over a
      rolling `apt install` / `brew install`, so CI uses the same commit-locked
      LLVM as dev and cannot drift. A dedicated backend CI job runs with
      `-DZOM_ENABLE_LLVM_BACKEND=ON` over the RFC 0016 discovery-gate fixtures
      (14/14) and the backend `llvm-translation-test`; frontend jobs stay OFF
      (fast, no LLVM), so `ZOM_ENABLE_LLVM_BACKEND` default-OFF remains the
      frontend contract while the backend dependency is stable where it is
      needed.
    System packages (apt.llvm.org / Homebrew) are acceptable only as a
    convenience fallback, never the source of truth - the locked commit is. This
    turns the current dev-only manual build into a commit-reproducible,
    CI-enforced, self-managed dependency, matching how Rust and Swift manage LLVM.

### O3 - Unblock the next wave through real governance and spec work
*Reachable now. G7 proved genuine per-owner review works and catches real
defects.*

- **KR3.1** RFC 0022 (flow-sensitive typing / null safety): REVIEW -> ACCEPTED
  via a real ten-owner review (no fabricated approval). **2026-08-27: HELD IN
  REVIEW on a genuine blocking defect (not fabricated to close).** A real review
  found RFC 0022 allocates diagnostics `ZOM4096`/`ZOM4097`/`ZOM4098` for
  null-safety and claims that range free, but the production registry
  `diagnostics-checker.def:150,152,154` now defines `ZOM4096
  ControlFlowSemanticsUnavailable` / `ZOM4097 VoidReturnSemanticsUnavailable` /
  `ZOM4098 ExpressionStatementSemanticsUnavailable`, with live conformance
  expectations asserting them (verified on disk). Accepting as written would
  double-allocate live, test-asserted codes. This is a design gap needing a
  normative re-allocation + snapshot re-review, not mechanical drift. A secondary
  producer-tag mismatch (`FlowRefinement` claimed after a non-existent
  `SignatureClassification = 0x15`) was also recorded. error-system OBJECT is
  recorded in the tracker; no owner was falsely marked approved. The stale
  recorded snapshot hash was mechanically reconciled to the current file
  (`1cbe543a...`). Follow-up: re-allocate the null-safety codes to a free range,
  fix the producer-tag reference, re-freeze, re-review.
- **KR3.2** RFC 0043 (link + executable): DRAFT -> REVIEW. **2026-08-27: HELD IN
  DRAFT (not review-ready).** RFC 0043's own Open Questions defer three
  substantive design decisions explicitly assigned "before REVIEW" (macOS SDK /
  Linux sysroot toolchain-discovery binding; which RFC 0010 failure rows cover
  linker-process failures; the native CI architecture lanes), all unresolved;
  and `discussion`/`tracking-issue` are both `TBD`. Structurally otherwise clean.
  A tracker `docs/rfc/tracking/0043-review-and-implementation.md` was created
  recording the hold, owner matrix, and blockers. Follow-up: resolve the three
  Open Questions and bind discussion/tracking, then DRAFT -> REVIEW.
- **KR3.3** Stretch-2 reachability verdict: a read-only investigation that
  decides do-or-drop on a checker-only `ErrorOperatorFact` slice. The verdict
  itself is the deliverable; a prior probe suggests raises-carrying calls are
  rejected before the operand type exists (likely unreachable), so this is not
  assumed shippable. **VERDICT 2026-08-27: DROP (code-traced).** The operator
  reaches the checker (parser accepts `?!`/`!!`; surface admission passes it via
  `isAdmittedErrorPostfix`), but the operand never has a formed error-union value
  type: `directCallableShape`/receiver-shape return none when `raises != none`
  (`body-checker.cc:1233,1387`), so a raising call forms no node-type fact;
  `ErrorUnionShapeFact` has zero producers (always `emptyFactMap`, verified at
  `body-checker.cc:3052`); the `ErrorOperator` production site
  (`body-checker.cc:2756-2785`) has no positive branch and always rejects
  (ZOM4032/ZOM4026 or a MissingRequiredFact invariant); and the cross-fact
  validator (`checked-facts.cc:1871-1904`) makes the two facts co-dependent and
  jointly unreachable. It is spec-blocked on RFC 0006 (an error-union value type
  + raising-call shape admission), a type-system increment far larger than
  G5/G6a-1 and governance-blocked, NOT a checker-only slice. The plan's earlier
  "authorized by RFC 0005" wording was inaccurate: RFC 0005 supplies the fact
  struct, not its producer.

### O4 - Toward a complete, usable toolchain (EXPANDED SCOPE 2026-08-27)

*Scope added mid-quarter on the goal "polish the compiler's capabilities to
completeness": end-to-end run, LSP, package manager, build tool, formatter,
debugger. This is a large surface; each KR is classified honestly against
on-disk reality, and - per this plan's governance rule - a feature whose RFC is
not yet IMPLEMENTING is written as "advance the RFC / land the reachable slice",
NOT "ship the feature", because implementation is governance-gated. Most of O4
realistically spans beyond Q4; the Q4 commitment is the reachable first slice of
each, with the rest scheduled into 2027 Q1.*

*Product-shape decision (2026-08-27): the toolchain is ONE front-end binary,
`zomc`, with subcommands (compile/run/build/test/add/fmt/...), matching Cargo,
Go, and SwiftPM. There is no separate package-manager or build-tool binary; the
empty `products/zomcrate/` and `products/zomforge/` placeholder directories were
removed. "Package manager" (KR4.3) and "build tool" (KR4.4) below name
capabilities of `zomc`, not distinct products. See
`docs/design/tooling/product-ecosystem.md`.*

- **KR4.1 End-to-end run (native binary).** Q4-reachable slice: extend the KR2.4 path with LLVM object
  emission (`TargetMachine::addPassesToEmitFile` -> `.o`) behind
  `ZOM_ENABLE_LLVM_BACKEND`, and drive RFC 0043 REVIEW -> ACCEPTED so linking is
  authorized. A real end-to-end `zomc run hello.zom` -> native executable needs
  RFC 0043 IMPLEMENTING (link + ToolchainClosure) and is the milestone this
  unblocks, not part of this KR. **Status 2026-08-30: COMPLETE.** Object emission
  and the RFC transition are committed; the downstream run path is tracked by
  O5.
- **KR4.2 LSP / IDE.** *Governance-blocked; RFC 0023 is REVIEW and no
  `tools/ide` or `tools/lsp` directory exists.* Q4-reachable:
  drive RFC 0023 REVIEW -> ACCEPTED via a real per-owner review, then land the
  first authorized slice (the IDE semantic-snapshot facade / document-version +
  cancellation core the RFC defines), not a full language server. **Status
  2026-08-30: PARTIAL.** RFC 0023 is IMPLEMENTING and its lexeme, recovery, live
  lexer bridge, trivia classification, and parse-eligibility foundations have
  landed, but no document-version core, semantic facade, `tools/ide`, or
  `tools/lsp` production path exists.
- **KR4.3 Package manager.** *Partially reachable; RFC 0012 is IMPLEMENTING and
  real `driver/package/` code exists (manifest, resolver, lockfile, archive,
  build-script plan).* Q4-reachable: continue the RFC 0012 implementation
  tracker's next slice (e.g. deterministic dependency resolution / lockfile or
  manifest surface end-to-end through `zomc`), with project-native tests.
- **KR4.4 Build tool.** *Overlaps RFC 0012 build-script + the driver.* Q4-reachable:
  a `zomc build` path that drives the package-compilation request through the
  existing driver to Built MIR for a multi-file package (frontend-complete build,
  no native emission yet - that waits on KR4.1). **Status 2026-08-30:
  CANDIDATE.** `zomc build` is registered with package-selection and compilation-
  policy options only, drives the frontend-only path through verified Built MIR,
  and passes the multi-module workspace plus lock update/replay integration gate
  in both sanitizer and LLVM builds. The change remains a worktree candidate
  until committed.
- **KR4.5 Formatter.** *Governance-blocked; RFC 0044 is DRAFT.* Q4-reachable:
  advance RFC 0044 DRAFT -> REVIEW (author its open decisions from prior art),
  not implement the formatter.
- **KR4.6 Debugger.** *Governance-blocked; RFC 0045 is DRAFT and depends on native
  output (KR4.1).* Q4-reachable: advance RFC 0045 DRAFT -> REVIEW only; real DAP
  work is post-backend and belongs in 2027 Q1.

Honest overall note: O4 is a multi-quarter program. What Q4 can truthfully
deliver is (a) the reachable first implementation slice where the RFC is already
IMPLEMENTING (KR4.3, KR4.4) or backend-adjacent (KR4.1 object emission), and (b)
genuine RFC status advances for the governance-blocked ones (KR4.2, KR4.5, KR4.6,
plus RFC 0043 for KR4.1). "Complete toolchain" is the destination; these KRs are
the honest first steps, and none will be reported done by fabricating an RFC
transition or a feature that is not built and verified.

### O5 - Make `zomc run` produce and execute a native binary (ADDED 2026-08-28)

*Promoted from the Follow-up list into a committed OKR after every O1-O4 KR
landed. This is the through-line that turns the verified object-emission slice
into a runnable program. RFC statuses are live as of 2026-08-29
(0043/0021/0010 IMPLEMENTING). Graded per the same honest rule as O4:
each KR grades on its reachable first slice or genuine advance; a governance- or
runtime-blocked KR grades "blocked, cause recorded", never failed-by-omission.
O5 is explicitly multi-quarter; the highest-leverage, no-new-gate first step is
KR5.1.*

- **KR5.1 Drive object emission from the CLI.** *Reachable now, no governance
  gate.* Replace `emitBinary()`'s `BinaryEmissionUnavailable` stub with the real
  path: take the session's `getOwnershipCheckedMirModules()`, lower through
  MIR -> LIR (`MirToLirLowering`) -> `LlvmTranslator`, and write the emitted
  native object to the `-o` output path, all behind `ZOM_ENABLE_LLVM_BACKEND`
  (the frontend build stays unaffected when it is OFF). First point `zomc
  compile --emit=binary -o x.o` puts a native artifact on disk. Q4-reachable
  slice: the single-function scalar shape the translator already emits, with a
  driver test that inspects the written object's ELF magic.
- **KR5.2 Widen backend lowering beyond the single-block shape.** *Reachable
  now.* The translator requires exactly one single-block integer-return
  function; generalize MIR -> LIR -> LLVM to the multi-block, multi-function MIR
  the frontend already produces (conditionals, loops, calls). Ordered after the
  shapes O1 lowers.
- **KR5.3 RFC 0043 ACCEPTED -> IMPLEMENTING + linking.** *Governance gate then
  code.* Advance RFC 0043 to IMPLEMENTING via a real per-owner review, then
  implement the `InvokeLinker` + ToolchainClosure contract: discover the host
  linker, build the canonical link plan, invoke it on the emitted object plus the
  runtime closure, and publish a verified executable + `.zom-artifact` manifest.
  **Status 2026-09-01: committed and pushed to `develop` (through `0286b894`).**
  The link/inspect/publish chain, the D5 and D1 audit repairs, and the KR5.3
  owner-review findings all landed; RFC 0043 is at hash `c2e366c8` with six
  subagent gates RECOMMEND (recorded as Re-Approval Evidence in the tracker, not
  as approvals). KR5.3 remains open pending the delegated owner re-approval
  decision recorded against `c2e366c8`.
- **KR5.4 `zomc run` end to end.** *Blocked on KR5.3.* Replace `rejectRun()` with
  compile -> link -> execute, gated on the host-compatibility predicate (a
  cross-target artifact publishes but refuses to run), plus a minimal runtime
  entry object so a single-function module links into a runnable executable.
  First true `zomc run hello.zom`. **Status 2026-08-30: CANDIDATE.** The Linux
  x86-64 scalar-module-initializer slice passes the backend-on
  `native-execution-cli` test in the current worktree; completion requires the
  candidate to land with KR5.3 and the integration gates.
- **KR5.5 A3 drop materialization.** *Reachable now that a LIR/backend consumer
  exists.* Add the MIR drop statement previously deferred to stay fail-closed.

### O6 - Advance the accepted governance surface (ADDED 2026-08-28)

*The RFCs accepted this quarter authorize implementation workstreams; O6 tracks
starting them. Each is independent of the O5 backend spine except where noted.*

- **KR6.1 RFC 0022 flow-typing -> IMPLEMENTING.** *Reachable (ACCEPTED).* Begin
  the flow-sensitive refinement / null-safety checker workstream the accepted RFC
  authorizes; land its first verified fact slice.
- **KR6.2 RFC 0044 formatter REVIEW -> ACCEPTED, then core.** *Reachable;
  backend-independent.* Complete the per-owner review, then implement the Doc-IR
  core and `zomc fmt` / `zomc fmt --check` (the lossless-snapshot dependency,
  RFC 0023, is already ACCEPTED).
- **KR6.3 RFC 0023 IDE/LSP first rollout slice.** *IMPLEMENTING (`ed8a797f`).* The
  recoverable lexeme partition codec + verifier landed as the first slice; the
  parser cutover, document versions, and IDE facade remain downstream.
- **KR6.4 RFC 0045 debugger REVIEW -> ACCEPTED.** *Governance; sequenced after
  native output.* Advance the review; real DAP work stays downstream of KR5.4.

### Grading intent

O1 is the committed core and is expected to grade fully. O2 grades on gate
progress: KR2.1 (attempted, outcome reported truthfully) and, if it clears, as
far down KR2.2-KR2.4 as the cleared gate allows. O3 grades on the two
transitions plus a recorded verdict. O4 (expanded scope) grades per-KR on the
reachable first slice or the genuine RFC advance, NOT on shipping the full
feature - it is an explicitly multi-quarter program whose Q4 commitment is the
honest first step of each capability. O5 and O6 (added 2026-08-28 after O1-O4
landed) grade the same way: each KR grades on its reachable first slice or
genuine advance, with KR5.1 the committed no-new-gate first step and the rest
sequenced by their gates. A blocked conditional KR grades as
"blocked, cause recorded", never as failed-by-omission and never as done. A
worktree-only implementation is a candidate, not complete. A narrower substitute
does not complete an explicitly named CLI or production-boundary deliverable.

---

## Initial Q4 goals (completed during the Q3 planning period)

This was the original four-goal scope. It remains as completion evidence, but
the O1-O6 OKRs and the 2026-08-30 rebaseline are authoritative for quarter-close
grading.

- **G1.** Primitive arithmetic and bitwise binary operations lower end-to-end
  (frontend, reachable now). **DONE** (`2ed20c94`).
- **G2.** Multi-statement function bodies with sequential local dataflow lower
  end-to-end (frontend, reachable now). **DONE** (`3f7f5189`, `76eaa630`).
- **G3.** RFC 0016 advances from REVIEW to ACCEPTED, unblocking backend review
  (governance). **DONE** (`26a30ada`). All eight required owners approved the
  `cfabf1a0` snapshot through a real per-owner review after two blocking
  objections were resolved (`.agents`->`.codex` routing; repository-wide
  `zom-v1`->`zom` codec regeneration across RFC 0016 and RFC 0010). Accepted
  snapshot `ec27f6d3`.
- **G4.** RFC 0021 enters formal REVIEW once G3 lands (governance). **DONE**
  (`a6cdee95`). With RFC 0016 ACCEPTED, RFC 0021's dependency gate cleared; it
  moved DRAFT->REVIEW with all nine bound upstream pins resynchronized.
  Required-owner review of the snapshot is now pending (owner work, not
  implementation).
- **Stretch.** ~~RFC 0006 error-union type representation slice begins~~
  **RETIRED - premise was wrong** (see Progress log 2026-08-25). The semantic
  error-union representation already exists and is implemented: it is
  `ErrorUnionShapeFact` over a canonical `Union` result, not a `TypeData`
  variant. RFC 0005 (IMPLEMENTING) freezes the 15-variant `TypeData` algebra and
  explicitly forbids a built-in error-union type, so adding one is unauthorized,
  not a reachable slice. What is genuinely open is error-operator *lowering*,
  which is backend/governance-blocked (RFC 0006 ABI descriptor consumes RFC 0010
  target selection; RFC 0046 panic ABI is DRAFT). A checker-only error-operator
  *fact* slice on top of the existing `ErrorUnionShapeFact` would be authorized
  by RFC 0005, but it is not this Stretch item and is not required to close Q4.

## Pre-rebaseline continuation goals (added 2026-08-26)

G1-G4 were complete before Q4 began. These goals record the next planning-period
slices that produced O1-O3. The O1-O6 OKRs and the rebaselined monthly schedule
now own remaining commitments.

- **G5. Nested arithmetic operands** (frontend, reachable now). Lower a binary
  whose operand is itself a binary node (`a + b * c`): extend the checker and
  HIR beyond the current parameter/local/scalar-literal operand set. Continues
  the G1/G2 fixed-shape-generalization pattern.
- **G6. Richer conditional and loop bodies** (frontend, reachable now - RE-SCOPED
  2026-08-26 into slices). An end-to-end probe found the monolithic goal is NOT a
  bounded fixed-shape generalization like G5: the mut-local write *value* is
  modeled scalar-literal-only across HIR (six interlocking module-global count
  formulas) and MIR (~8 hand-specialized write builder/verifier functions,
  single-`UserLocal`, constant-only). The fail-closed baseline (loop body must be
  empty; write RHS must be a scalar literal) is kept until a slice lands soundly.
  Ordered slices:
    - **G6a-1** (smallest sound): per-write literal-XOR-reference value descriptor
      in the HIR pending model + parameter-local declaration and `placeUse` in the
      single-mut-local write MIR builders/verifiers. Target
      `fun f(a: T) -> T { mut x = <lit>; x = a; return x; }`.
    - **G6a-2**: binary RHS (needs the fixed 2-node-ids-per-write stride
      generalized first).
    - **G6b**: redesign `HirLoopStatement` to carry body statements, then widen
      loop admission to a non-empty body that mutates locals.
- **G7. RFC 0021 required-owner review** (governance, in-project review). Run the
  ten-owner review of RFC 0021's REVIEW snapshot the same way G3 was run (real
  per-owner review, no fabricated approval). On unanimous approval, transition
  REVIEW -> ACCEPTED; do not skip to IMPLEMENTING (the LLVM toolchain gate in G8
  precedes implementation).
- **G8. LLVM 22.1.8 toolchain alignment** (operational prerequisite). RFC 0016 is
  ACCEPTED but cannot enter IMPLEMENTING until the environment provides LLVM
  `22.1.8` (currently `19.1.5`) through the accepted CMake/CI transaction; ambient
  LLVM must not be admitted. This gates the entire backend milestone.
- **Stretch-2. Checker-only error-operator fact slice** (reachable now,
  authorized by RFC 0005). Produce/verify `ErrorOperatorFact` on top of the
  existing `ErrorUnionShapeFact`. Adds no type and publishes no
  backend/governance-blocked artifact; error-operator *lowering* stays blocked.

The original deferrals were superseded as their gates cleared: LIR/LLVM moved
into O2, link/run into O5, and accepted tooling work into O6. The current blocked
and partial boundaries are recorded in the status section, not in this historical
list.

## Month-by-month (rebaselined 2026-08-30)

The original October-December sequence was completed during the Q3 planning
period and no longer describes the remaining work. Q4 now closes exact
deliverables rather than repeating already-landed slices.

### October 2026 - land the native execution transaction

- Land the KR5.3 D5 and D1 audit-repair candidate as a reviewable commit series.
- Land the KR5.4 Linux x86-64 scalar `zomc run` slice on the consuming
  `linkAndPublish` path.
- Run the full sanitizer suite, the LLVM-backend suite, RFC/architecture gates,
  and the backend CI job after push; do not promote candidate status on focused
  tests alone.
- Update the live IR design notes and RFC trackers so no current document claims
  that LIR, LLVM, object emission, linking, formatter CLI, or native execution is
  absent when the production path exists.

### November 2026 - close the exact CLI and syntax-snapshot boundaries

- Land the verified KR4.4 `zomc build` candidate as the package-to-Built-MIR
  command path. It must retain its output-free command surface and multi-module
  workspace integration gate.
- Advance KR6.3 through the parser event stream, `RecoverableSyntaxTree`, and
  verified AST bridge. A pure-data codec does not complete the semantic-snapshot
  KR.
- Add the first document-version and cancellation-owned snapshot boundary only
  after the parser/CST replacement is production-wired and independently
  verified.

### December 2026 - semantic facade, blocked-KR decisions, and quarter close

- Complete KR4.2/KR6.3 only when a production IDE semantic facade consumes a
  versioned verified snapshot. A full LSP server remains outside the KR.
- Reassess KR6.1 after the subtype/coercion/HIR/MIR dependency chain is
  production-reachable. If no non-dead first flow-fact slice exists, close the
  quarter as Blocked with the exact dependency rather than introducing a free
  `isSubtype` helper with no downstream consumer.
- Keep KR5.5 Blocked until a drop-aware LIR/runtime consumer and structural MIR
  cleanup verifier exist. Do not add an unproduced `Drop` statement kind.
- Publish the final 27-KR score, remote CI evidence, remaining platform limits,
  refreshed RFC trackers, and the 2027 Q1 plan.

## Explicitly out of scope after the 2026-08-30 rebaseline

- **General native backend and ABI coverage.** The verified Linux x86-64 scalar
  slice does not authorize general MIR -> LIR legalization, general entry-point
  semantics, cross-module symbol lowering, complete calls, aggregates, drop ABI,
  macOS execution, or AArch64 execution.
- **Error-operator lowering (`?!` / `!!`) and RFC 0046 panic ABI.**
  Backend/governance-blocked. The semantic error-union representation already
  exists (`ErrorUnionShapeFact` over a canonical `Union`, RFC 0005); what is
  blocked is *lowering*: RFC 0006's error-union ABI layout descriptor is
  target-dependent (consumes RFC 0010 target selection), and RFC 0046 panic ABI
  is DRAFT with no approvers. A checker-only error-operator *fact* slice on top
  of the existing shape fact is authorized by RFC 0005 but is not required this
  quarter.
- **A3 drop materialization.** No MIR drop statement kind and no LIR/backend
  consumer; adding one now violates fail-closed. Deferred until the backend
  exists.
- **`as!` forced cast.** No parser support and no lowering target; adding it now
  is spec drift.

## Constraints this plan respects

- No forward compatibility, no version-suffixed contracts, fail-closed on
  unimplemented slices (`.codex/rules/design-principles.md`).
- No blocked IR or runtime artifact is published before its RFC authorizes it.
- Governance transitions are never fabricated; a DRAFT/REVIEW RFC is a real
  blocker, not a formality to route around.

## Tracking

Update the goal checkboxes below as items land; keep the RFC status table above
in sync with `docs/rfc/` frontmatter at each month boundary.

- [x] G1 - primitive arithmetic/bitwise lowering
- [x] G2 - multi-statement function bodies (N sequential `let` locals with
  scalar-literal / aggregate / identifier / primitive-binary initializers)
- [x] G3 - RFC 0016 REVIEW -> ACCEPTED (unanimous per-owner review, `26a30ada`)
- [x] G4 - RFC 0021 into REVIEW (dependency gate cleared, `a6cdee95`)
- [x] Stretch - RETIRED: premise wrong (error-union representation already
  exists as RFC 0005 `ErrorUnionShapeFact`; a `TypeData` variant is forbidden)
- [x] G5 - nested arithmetic operands: `let z = a + b * c;` lowers end-to-end
  (one operand, one level; nested inner binary -> synthesized Temporary local +
  two assignments; no new codec byte)
- [x] G6 - richer conditional/loop bodies + local reassignment (frontend;
  RE-SCOPED into G6a-1 / G6a-2 / G6b; ALL THREE LANDED. The end-to-end probe that
  showed the monolith needs HIR fixed-id restructuring was right; the restructure
  was done incrementally per slice, each fail-closed and verified)
  - [x] G6a-1 - a mutable-local write with a parameter-reference RHS lowers
    end-to-end (`mut x = 0; x = a; return x;`); per-write literal-XOR-reference
    value descriptor; parameter local + placeUse in MIR; no id shift, no new
    codec byte
  - [x] G6a-2 - binary RHS: `mut x = 0; x = a + b; return x;` lowers end-to-end
    (Arithmetic/Comparison rvalue over param/literal operands); operand nodes in
    a trailing id region so literal/reference write ids are unchanged; no new
    codec byte [O1/KR1.1]
  - [x] G6b - non-empty loop body: `while (cond) { x = <lit|param|binary>; }`
    lowers to a reducible four-block CFG (`HirLoopStatement` gained a `body`
    node-id list; empty-body loop unchanged); no new codec byte [O1/KR1.2]
- [x] RFC 0009 call-dispatch widening (multi-arg or method call to MIR) [O1/KR1.3]
  - landed `7564b86a`: a same-module direct call with N arguments lowers to a
    MIR Call with N operands. The real blocker was a single-parameter-fixed
    parameter-return function builder/verifier (a multi-param callee failed with
    ZOM9944); generalized to N parameters, single-param case byte-identical.
- [x] Lowerable-constructs inventory + baseline delta at quarter close [O1/KR1.4]
  - landed `1685b512` docs/design/ir/lowerable-constructs.md: 22 constructs at
    HEAD vs 6-family 2026-08-25 baseline (+6 net-new: G1/G2/G5/G6a-1/G6a-2).
    Finding: call admission out-runs proven lowering (feeds KR1.3).
- [x] G7 - RFC 0021 required-owner review -> ACCEPTED 2026-08-26 (ten owners;
  one mechanical 56-byte oracle-label defect found and fixed; no IMPLEMENTING
  pointer, LLVM 22.1.8 gate still precedes implementation)
- [x] G8 - LLVM 22.1.8 toolchain alignment (backend prerequisite) [O2/KR2.1]
  - [x] KR2.1a - RE-ASSESSED 2026-08-27: NOT separately reachable. The accepted
    RFC 0016 CMake contract mandates an unconditional exact-22.1.8 configure gate
    whose positive fixture needs a real 22.1.8; wiring it in a 19.1.5-only env
    breaks the frontend build and an isolated variant would be drift or dead code.
    Collapses into KR2.1b.
  - [x] KR2.1b - DONE 2026-08-27 via from-source build. apt.llvm.org prebuilt
    hit a glibc 2.38-vs-2.36 floor; resolved by building llvmorg-22.1.8 (commit
    ca7933e47d3a) from source on this glibc-2.36 host (ninja -j52), installed to
    $HOME/toolchains/llvm22. Verified: llvm-config 22.1.8, X86+AArch64, llc runs,
    11 components resolve, verifyModule program links+runs. Unblocks KR2.1a +
    KR2.2-2.4.
- [x] RFC 0016 + RFC 0021 -> IMPLEMENTING [O2/KR2.2] (cb8e1e3b, f9c670bf)
- [x] First LIR codec-foundation slice [O2/KR2.3] (fabf520d; live encoder
  reproduces the RFC 56-byte oracle 03106c34...)
- [x] Minimal MIR -> LIR -> LLVM IR + LLVM verifier [O2/KR2.4] (4f078d78; scalar
  module initializer -> verified `ret i32 42`, behind ZOM_ENABLE_LLVM_BACKEND)
- [~] Self-managed (vendored) LLVM 22.1.8 dependency [O2/KR2.5] (e78cc98e) -
  third_party/llvm/llvm-lock.json locks tag+commit ca7933e47d3a as the source of
  truth; scripts/provision-llvm.py idempotently builds it from source;
  scripts/check-llvm-lock.py fails closed on lock<->gate drift; a check-backend-llvm
  CI job (ubuntu-24.04, cache keyed on the commit) runs the gate fixtures +
  llvm-translation-test with the backend ON, frontend jobs OFF. Locally verified;
  CI job authored but NOT yet run (green only after push). The source lock,
  provisioner, and local gate are complete; the KR remains PARTIAL until the
  remote backend job passes. The macOS lane is outside this KR's current Linux
  backend slice.
- [x] RFC 0022 REVIEW -> ACCEPTED (real ten-owner review) [O3/KR3.1]
  - [x] 2026-08-27: first review HELD on a genuine ZOM4096-4098 code collision +
    producer-tag mismatch.
  - [x] 2026-08-27 `a081de02`: both defects fixed (codes re-allocated to the free
    ZOM4100-4102; producer-tag corrected to FlowRefinement=0x14 after Constant=
    0x13), re-frozen, re-reviewed, and ACCEPTED on unanimous nine-owner approval.
    KR3.1 COMPLETE. implementation stays TBD (no IMPLEMENTING pointer).
- [x] RFC 0043 DRAFT -> REVIEW [O3/KR3.2]
  - [x] 2026-08-27: first pass HELD in DRAFT (a determinability cross-check found
    the three before-REVIEW Open Questions are not settled by accepted upstream).
  - [x] 2026-08-27 `55d2b60b`: those three are decisions the RFC's own authors
    must make - authored each from mature prior art (toolchain-discovery
    ToolchainClosure a la Clang/Rust/Zig; RFC 0010 failure-algebra extension for
    linker/publication phases; a concrete native-vs-cross CI lane matrix),
    cleared Open Questions to None, and advanced DRAFT -> REVIEW. approvers empty,
    no ACCEPTED fabricated. KR3.2 COMPLETE (transition earned honestly).
- [x] Stretch-2 reachability verdict - checker-only error-operator fact
  (do-or-drop decision; RFC 0005 authorized if reachable) [O3/KR3.3]
  - [x] VERDICT 2026-08-27: DROP. Spec-blocked on RFC 0006 (no error-union value
    type; raising calls form no callable shape at body-checker.cc:1233;
    ErrorUnionShapeFact has zero producers). Not a checker-only slice.
- [x] End-to-end run: LLVM object emission + RFC 0043 REVIEW->ACCEPTED [O4/KR4.1]
  - [x] 2026-08-28 `0ecea62f`: RFC 0043 REVIEW->ACCEPTED (the governance
    half). Six-owner review: all five dependency RFCs (0006/0010/0012/0016/0021)
    are IMPLEMENTING; verified the RFC 0010 failure-algebra extension
    (LinkPlanConstruction/LinkerInvocation/ExecutablePublication + InvokeLinker)
    is a clean no-collision extension past ObjectEmission with no new diagnostic
    code; every README ACCEPTED gate met. Linking is now design-authorized.
  - [x] 2026-08-28 `10ef73b2`: LLVM object emission implemented. The translator
    runs the RFC 0021 ObjectEmission phase (legacy PassManager +
    `addPassesToEmitFile(CodeGenFileType::ObjectFile)` into a `raw_svector_ostream`,
    after verifyModule), and `LlvmTranslationResult` carries the object bytes
    behind the isolation wall. `llvm-translation-test` asserts the scalar module
    initializer lowers MIR -> LIR -> verified LLVM IR -> a non-empty native ELF
    object on this x86_64-linux-gnu host, against the provisioned LLVM 22.1.8
    under `ZOM_ENABLE_LLVM_BACKEND` (2/2 backend tests pass). Both KR4.1 halves
    (object emission + RFC 0043 ACCEPTED) are done. KR4.1 COMPLETE.
  - A full `zomc run hello.zom` -> linked native executable is the downstream
    milestone this unblocks; it needs RFC 0043 IMPLEMENTING (link + ToolchainClosure)
    and was never a Q4 guarantee per the KR text.
- [~] LSP: RFC 0023 REVIEW->ACCEPTED + first IDE semantic-snapshot slice [O4/KR4.2]
  - [x] 2026-08-28 `d7a42493`: RFC 0023 REVIEW->ACCEPTED. The RFC 0022
    dependency cleared (accepted 2026-08-27); rechecked the
    VerifiedFlowToolingProjection (RFC 0022) and BindOwnerBody/OwnerBodySyntax/
    StableBodyOwnerKey (RFC 0019) cross-references; found and fixed one factual
    defect (the stale `--syntax-only` CLI claim -> `--check`); all eight required
    owners approved. This completes the governance half only. RFC 0023 later
    entered IMPLEMENTING and landed CST foundations under KR6.3, but the named
    semantic-snapshot facade/document-version slice remains absent. KR4.2 is
    PARTIAL.
- [x] Package manager: next RFC 0012 (IMPLEMENTING) tracker slice [O4/KR4.3]
  - [x] 2026-08-28: verified on disk that the tracker's named "next normalization
    boundary" - workspace-member expansion plus the complete collision and
    permutation matrix - is already implemented, wired, and green. The
    `workspace-normalizer.{h,cc}` module (`normalizeWorkspace`, `sortInputs`,
    `WorkspaceMemberInput`/`NormalizedWorkspace`) is called from
    `package-compilation-request.cc`, and `package-workspace-normalizer-test`
    passes (5 cases: canonical order, input-order independence/permutation,
    missing+nested rejection, duplicate package name, duplicate canonical path -
    covering all four emittable `ManifestIssue` variants). RFC 0012's P0-P9 are
    all implemented per its tracker; the only remaining item is P9's final
    combined release rerun, which requires the privileged Linux sandbox
    integration and downstream RFC 0008/0010 completion - environment/dependency
    gated, the same class as KR4.1's LLVM-env gate, not a bounded frontend slice.
    KR4.3's reachable slice is COMPLETE; the remainder is blocked-with-cause,
    graded per the O4 rule (never failed-by-omission).
- [~] Build tool: `zomc build` drives a multi-file package to Built MIR [O4/KR4.4]
  - [x] 2026-08-28 `5dcdc80e`: proved a two-module `app` package (main
    importing a child module, each with a scalar-return function) lowers BOTH
    modules end-to-end to Built MIR via a new ztest in
    `compiler-session-package-test.cc`. `getVerifiedHirModules().size() == 2` and
    `getOwnershipCheckedMirModules().size() == 2`, each function ending in a
    scalar `Return`/`Constant`. The capability already existed (the session's
    per-module MIR loop at `compiler-session.cc:3455`); the gap was coverage -
    every prior MIR-reaching test was single-module. No production change; the
    driver already drives multi-module packages through `compile --package`.
  - [~] 2026-08-30 candidate: `zomc build` is now a first-class command that
    shares package selection and compilation-policy options without exposing
    `--emit`, output selectors, or `--check`. It runs the frontend-only action
    through `CompilerSession::checkSources()`, which publishes verified HIR and
    Built MIR. `package-workspace-build` constructs an `app` package with
    `src/lib.zom` importing `src/child.zom`, exercises lock update and locked
    replay, and passes under both sanitizer and LLVM builds. The package
    architecture gate now requires the command and integration test. The full
    sanitizer suite passes 308/308 with `package-workspace-build` included.
    KR4.4 is CANDIDATE until this series lands.
- [x] Formatter: RFC 0044 DRAFT->REVIEW [O4/KR4.5]
  - [x] 2026-08-28 `2ea758b2`: authored the missing layout engine (Wadler/Lindig
    Doc-IR algebra + pinned 100-col width) and resolved both before-REVIEW Open
    Questions from prior art (range-expansion boundary-token ownership;
    cross-platform atomic rename); DRAFT -> REVIEW earned, approvers empty,
    decision TBD, no ACCEPTED fabricated. KR4.5 COMPLETE.
- [x] Debugger: RFC 0045 DRAFT->REVIEW (real DAP is post-backend, 2027 Q1) [O4/KR4.6]
  - [x] 2026-08-28 `7c5a0dfc`: resolved all three before-REVIEW Open
    Questions from prior art (canonical DWARF source-name encoding; lldb-dap
    executable endpoint over the LLDB library API; scalar-only initial variable
    surface); DRAFT -> REVIEW earned, approvers empty, decision TBD. Acceptance
    stays gated on the backend RFCs (0043 link/publication). KR4.6 COMPLETE.

### O5 - runnable `zomc run` (added 2026-08-28; multi-quarter)
- [x] Drive object emission from the CLI: `emitBinary()` lowers the session MIR
  through LIR -> `LlvmTranslator` and writes the `.o` to `-o` [O5/KR5.1]
  - [x] 2026-08-28 `17ee4518`: `zomc compile --emit=binary -o out.o` on
    `const answer: i32 = 42;` writes a 776-byte elf64-x86-64 object (llvm-objdump
    shows `.text` + `zom.module_init`). Behind ZOM_ENABLE_LLVM_BACKEND; fails
    closed with `-o`-required and a shape diagnostic outside the one-function
    scalar module-initializer slice (wider shapes = KR5.2). Frontend-only build
    keeps BinaryEmissionUnavailable. New backend-gated `object-emission-cli`
    asserts the ELF magic; `native-execution-unavailable-cli` learns
    `--binary-available` so its ZOM6007 assertion is frontend-only. Both builds
    green. KR5.1 COMPLETE.
- [x] Widen backend lowering beyond the single-block integer-return shape to the
  multi-block/multi-function MIR the frontend produces [O5/KR5.2]
  - [x] 2026-08-28 `a78a1df5`: width axis proven - i16 and i64 scalar module
    initializers lower MIR -> LIR -> verified LLVM (`ret i16 7`, `ret i64 9`) ->
    native ELF object (the path already parameterized on the LIR carrier width;
    added coverage).
  - [x] 2026-08-28 `b39a9eab`: first multi-block widening landed - a
    boolean-conditional four-block diamond (`if cond { return A } else { return
    B }`) lowers MIR -> LIR -> verified LLVM -> native ELF object. The LIR model grew from a
    terminator-only block to block-with-statements + a result-local slot +
    `Goto`/`CondBranch`/`ReturnLocal` terminators + function parameters
    (`compiler/lir/lir-module.{h,cc}`); `MirToLirLowering::lowerConditionalReturn`
    re-checks the `validConditionalReturnFunction` shape and maps SwitchInt(bool)
    -> CondBranch, arm Assign -> LIR Assign, place-use Return -> ReturnLocal
    (`compiler/lir/mir-to-lir.{h,cc}`); the LLVM translator emits an i1 parameter,
    an alloca for the result, `br i1`, a store per arm, and load+ret at the join,
    keeping the mandatory `verifyModule` and object emission
    (`compiler/backend/llvm/llvm-translator.cc`). New `llvm-translation-test` case
    asserts `br i1`, `alloca`, `store i32 7`/`store i32 9`, `ret i32`, and ELF
    magic (4/4 backend tests pass under the provisioned LLVM 22.1.8; frontend-only
    build unaffected). Constant arms only in this slice (a parameter-place-use arm
    would need an integer parameter, not the sole boolean discriminant).
  - [x] 2026-08-28 `ddb695ca`: reducible while-loop landed - a four-block loop
    (`while cond {} return exitValue`) lowers MIR -> LIR -> verified LLVM ->
    native ELF object through a generalized emitter. The LLVM translator's
    diamond path was refactored into a generic two-pass multi-block emitter
    (pass 1: one LLVM block per LIR block + the result alloca; pass 2: each
    block's Assign stores then its terminator), so `Goto`/`CondBranch`/
    `ReturnLocal`/`ReturnInteger` lower uniformly for both the diamond and the
    loop (with its `body -> header` back-edge and single-arm header
    `SwitchInt -> CondBranch`). `MirToLirLowering::lowerLoopReturn` re-checks the
    `validLoopReturnFunction` shape. New `llvm-translation-test` loop case asserts
    `br i1`, `alloca`, `store i32 5`, `ret i32`, ELF magic (5/5 backend tests
    pass; frontend build unaffected).
  - [x] 2026-08-28 `0e1b8198`: comparison-driven conditional landed - an
    `if a == b { return X } else { return Y }` diamond lowers MIR -> LIR ->
    verified LLVM -> native ELF object. This added the LIR `Compare` statement
    (-> LLVM `icmp`) and restored the `localUse` operand; the translator was
    generalized to an all-locals-to-alloca model (every parameter and body local
    gets an alloca; parameters store their incoming argument; Assign/Compare
    store; CondBranch loads its boolean condition local; ReturnLocal loads the
    result), threading N integer parameters as LLVM args and branching on a
    computed boolean temporary (not just a parameter). `lowerEqualityConditional
    Return` re-checks the `validEqualityConditionalReturnFunction` shape. New
    `llvm-translation-test` case asserts `icmp eq`, `br i1`, `store i32 3`/
    `store i32 4`, `ret i32`, ELF magic (6/6 backend tests pass; frontend build
    unaffected). Constant arms only in this slice.
  - [x] 2026-08-28 `feb4469f`: parameter-valued arm landed - a conditional whose
    arm returns an integer parameter (`if cond { return v } else { return <lit> }`)
    lowers MIR -> LIR -> verified LLVM -> native ELF object. `lowerConditional
    Return` was generalized to N parameters (a boolean discriminant plus integer
    value parameters), finding the condition parameter from the SwitchInt
    discriminant and lowering a parameter-place-use arm to a `localUse` operand
    that the all-alloca emitter loads. New `llvm-translation-test` case asserts
    `br i1`, `store i32 8`, `ret i32`, ELF magic (7/7 backend tests pass).
  - [x] 2026-08-28: same-module direct call landed - a zero-argument call
    `fun f() -> i32 { let x = g(); return x }` with a defined callee
    `fun g() -> i32 { return 5 }` lowers to a two-function LIR module and
    verified LLVM (a `call i32` to the module-local defined `zom.callee`) ->
    native ELF object. The LIR gained a `Call` terminator (callee module-local
    index + destination local + normal target); `lowerCallModule` lowers the
    verified `validLocalCallReturnFunction` caller plus its
    `validScalarReturnFunction` callee into one module; the translator was
    generalized to N functions (declare all, then define each; a Call emits a
    `CallInst` to the pre-declared callee, stores the result, and branches).
    Both functions are defined - no external/synthetic callee symbol is invented
    (module-local symbols, the same documented boundary as `zom.module_init`).
    New `llvm-translation-test` case asserts `call i32`, `zom.caller`/
    `zom.callee`, `ret i32 5`, ELF magic (8/8 backend tests pass; frontend build
    unaffected). Zero-argument call in this slice; multi-argument marshaling and
    the cross-module symbol-naming contract are later slices.
  - [x] 2026-08-29 `45dd8fe6`: first argument-carrying call landed - a same-module
    direct call passing one integer constant to a one-parameter callee that
    returns its parameter (`fun id(x) -> i32 { return x }`, `fun f() -> i32 { let
    r = id(9); return r }`) lowers to a two-function LIR module and verified LLVM
    (`call i32 @zom.callee(i32 9)`) -> native ELF object. Both the arg-carrying
    call MIR (KR1.3) and the parameter-return callee
    (`mir::validParameterReturnFunction`) are already reachable frontend shapes,
    so this is a non-dead widening. The LIR `Call` terminator gained an optional
    single integer-constant argument (`callFunctionWithArgument` +
    `callHasArgument`/`callArgument`); `lowerCallModuleWithArgument` admits the
    one-argument caller plus the parameter-return callee; the translator emits the
    call argument and accepts a single-block `ReturnLocal` callee. New
    `llvm-translation-test` case asserts `call i32`, `i32 9`, ELF magic (9/9
    backend tests pass; frontend build unaffected). One integer constant argument
    in this slice; multi-argument vectors, non-constant arguments, and the
    cross-module DefId->symbol contract remain later slices.
  - Status: all five multi-block MIR shapes the frontend produces (diamond,
    reducible loop, comparison condition, parameter arm, same-module call) now
    lower MIR -> LIR -> verified LLVM -> native ELF object through the generic
    all-alloca emitter, and the same-module call now threads one integer-constant
    argument. These lowerings are exercised by backend tests, not yet by a
    production driver: `zomc emitNativeObject` still selects only
    `lowerScalarInitializer`, so the call/conditional/loop lowerings are
    library-level slices with a backend test consumer, NOT a wired production
    path. A 2026-08-29 review also found and fixed a callee-identity bug: both
    `lowerCallModule` and `lowerCallModuleWithArgument` now require
    `call.callee == callee.owner` (a caller targeting a different definition was
    previously wired to the callee anyway). Remaining widening (multi-argument
    vectors, non-constant call arguments, cross-module calls with a real
    DefId->symbol contract, wider integer switches) plus the production
    driver-selection wiring is incremental on this foundation and sequenced as
    later backend work.
- [~] RFC 0043 ACCEPTED -> IMPLEMENTING + `InvokeLinker`/ToolchainClosure linking
  [O5/KR5.3] (governance gate, then code)
  - [x] 2026-08-28 `4faf0b74`: RFC 0043 ACCEPTED -> IMPLEMENTING, with the first
    authorized slice landed as evidence (Implementation Plan step 3, "without
    invoking a linker"). The closed failure algebra was extended in code (`IrFailurePhase`
    LinkPlanConstruction/LinkerInvocation/ExecutablePublication = 0x11/0x12/0x13,
    `BackendOperation::InvokeLinker` = 0x0b; append-only, byte-stable), and
    `compiler/ir/link-plan-codec.{h,cc}` implements the `ToolchainClosure`/object/
    runtime/argument records (no public aggregate init), the domain-separated
    length-framed `LinkPlanId` codec, and the independent `LinkPlanVerifier`
    enforcing the six numbered invariants, each rejection mapped to a
    `LinkPlanConstruction` failure row. `link-plan-codec-oracle-test` freezes a
    deterministic 503-byte minimal-plan oracle + `LinkPlanId`
    `287f421b...52a405` and a fail-closed mutation matrix (11/11 pass under the
    frontend sanitizer build; no linker, no filesystem, no LLVM linkage).
    `check-rfc.py` passes with the IMPLEMENTING transition.
    (2026-08-29 refinement, landed `320a2a01`: adversarial review authorized
    removing the generic argument surface entirely - see RFC 0043 Status History
    2026-08-29 - because the free-form record allowed a snapshot bypass and has
    no production producer. The codec/verifier/oracle deletion landed in D3b
    Slice 2 and the oracle was regenerated from the live encoder to 469 bytes /
    `LinkPlanId` `8e9a5cf7...5d60ca74`, so the 503-byte/`287f421b...` constants
    above describe the superseded historical shape.)
  - [x] 2026-08-28 `301d8280`: the `.zom-artifact` manifest data model landed as a
    pure-data slice - `compiler/ir/executable-manifest-codec.{h,cc}` defines the
    `VerifiedExecutableManifest` value type + `ExecutableManifestId`, the
    domain-separated `zom.executable-manifest` canonical codec (reusing the
    link-plan framing + `LinkPlanId`), and the independent
    `ExecutableManifestVerifier` (normalized in-root destination, non-zero byte
    count, non-empty target/toolchain identity, ordered duplicate-free input
    digests), each rejection mapped to an `ExecutablePublication` failure row
    (no failure-algebra change; the phase already exists). Frozen 264-byte oracle
    + field-sensitivity + fail-closed matrix, 7/7; no linker, no filesystem.
  - [x] 2026-08-29 scope split: KR5.3 is a small workstream, not one bounded
    slice. The review verified that `libraries/zc`
    has no public child-process API and there is no production host-toolchain
    discovery path, so "closure discovery + spawn + linker invocation +
    executable verifier + atomic publication" crosses processes, the
    filesystem, platform ABI, the failure algebra, and a publication
    transaction. It is split into ordered bounded slices, each its own commit:
    1. **argv-first child-process primitive (zc).** A generic, shell-free
       executor owning only argv/cwd/env/stdio/termination and a structured
       result; frozen result semantics (spawn failure, signal, exit code,
       stdout/stderr size cap) and an argv-fidelity test. No linker concept
       enters the base library.
    2. **host `ToolchainClosure` discovery + independent verifier (driver).** A
       single-platform fail-closed slice; an unsupported host rejects with no
       ambient fallback. The environment allowlist lives in the driver layer.
    3. **Verified `LinkPlan` -> canonical driver argv + temp-output cleanup
       (driver).** Consumes the landed link-plan codec (`4faf0b74`).
    4. **executable inspection + manifest construction + recoverable manifest-last
       publication (driver).** `InvokeLinker` failure mapping and stderr
       truncation/redaction live here. Consumes the landed manifest codec
       (`301d8280`).
    The link-plan codec (`4faf0b74`) and the manifest codec (`301d8280`) are
    the two pure-data foundations already in place. Layering guardrail: `zc`
    owns the generic mechanism only; all linker semantics stay in the RFC 0043
    driver layer.
- [~] `zomc run` end to end: replace `rejectRun()` with compile->link->execute +
  minimal runtime entry [O5/KR5.4] (blocked on KR5.3)
  - [x] 2026-08-28 `6f3fbd8d`: the host-execution compatibility gate landed as a
    pure-data slice - `compiler/ir/host-execution-profile.{h,cc}` defines
    `HostExecutionProfile` (the five RFC 0043 dimensions: OS, CPU architecture,
    object format, pointer width, required ABI capabilities) and
    `runCompatibility`, the total comparison `zomc run` applies before spawning
    (equal OS/arch/format/pointer width, host ABI superset), reporting the first
    violated dimension via the closed `HostMismatchReason`. 8/8; no subprocess, no
    filesystem.
  - [~] 2026-08-29: the RFC 0043 KR5.3 link-driver pieces landed as five bounded
    commits, but a 2026-08-29 adversarial review (Codex) found four blockers that
    keep the spine PARTIAL, not complete - the pieces exist and pass focused
    tests, but several tests locked incorrect behavior as expected, so the spine
    is not yet closed. The commits: (1) `ce035381` generic shell-free
    child-process primitive `zc::SubprocessCommand`/`Result` - layering held (no
    linker semantics in zc) but two generic-contract bugs remain OPEN: reaching
    the capture cap closes the pipe and can SIGPIPE a valid child (must
    drain-and-discard instead), and the Inherit env branch ignores `env()`
    entries (contract mismatch); interior-NUL guards missing. (2) `c00bd8bf`
    `discoverToolchain` - OPEN: the digested file (searchRoot/relativePath) is not
    bound to the recorded/executed path (caller supplies an independent
    `recordedPath`/`linkerAbsolutePath`), allowing digest-A-record-B; no pre-spawn
    re-verification, so it is not yet a verified closure. (3) `1d838335`
    `expandLinkPlanToInvocation` - OPEN: the canonical argv omits
    `closure.crtObjects()` and `closure.defaultLibraries()`, so it cannot produce
    a normal executable; the test wrongly froze their absence. (4) `b5c15d7c`
    `inspectExecutableFormat` + the first publication helper - format check is fine,
    but publication is NOT atomic: it commits the executable then the manifest, and a
    second-commit/dir-sync failure leaves the executable published (the code
    comment itself admitted a partial commit is possible). Then `08952eee`
    `invokeLinker` - OPEN: takes independent argv/cwd/read-path instead of a
    verified plan + fresh temp output, does not verify the output against the
    plan, does not ensure no stale pre-spawn output, does not clean partial output
    on failure, and returns a custom result not mapped to RFC 0010
    `IrOperationResult`/`IrFailurePhase::LinkerInvocation`. Fixes are in progress
    in Codex's recommended order (publication all-or-neither + failure injection;
    argv CRT/default-lib; discovery path<->digest identity + pre-spawn reverify;
    invokeLinker verified-plan/fresh-temp + cleanup + RFC mapping; subprocess
    capture/env/NUL contracts; call callee-identity + shape gate). Each fix lands
    with a test covering the mutation/failure branch the prior test missed.
  - [~] 2026-08-29: the four blockers above were closed by a redesign (not leaf
    patches), landed and reviewed as a reviewable-design-first chain. The approved
    shape lives in `docs/design/ir/link-publication-transaction.md` (D1-D5) and RFC
    0043's refined "Linker Driver Invocation" / "Executable Verification And
    Publication" sections. Landed slices: D2 `VerifiedSysroot` (binds the read root
    to its canonical identity); D3/D3b input snapshot + exec-by-descriptor (every
    input re-verified into a transaction-private root and consumed by fd, driver
    `execveat`'d by its snapshot handle, so no path<->digest split and no stale
    pre-spawn output); the generic argument surface removed (`320a2a01`, driver
    derives canonical argv from closed structural fields, fixing the CRT/default-lib
    omission); and D4 (`3764b2cb` + review-fix `8faec9da`) - the transaction-owned
    output candidate: `linkExecutable` takes the plan by value, writes `-o` to
    `<root>/output-candidate` (never the final path), captures the output's exact
    `StableFileIdentity`/size/SHA-256 digest through one no-follow read-only handle,
    enforces regular/non-empty/`st_nlink == 1`, transfers the live root into a
    move-only `LinkedOutputCandidate` on success, and cleans only the transaction
    root on any rejection. A per-call `SpawnAttemptObserver` gives a drift-proof
    "never spawned" proof (the old in-tree marker was cleaned with the root).
    The D1 implementation candidate landed as `9442ffdc`, but the 2026-08-29
    adversarial audit (`docs/reports/zom-link-publication-audit-2026-08-29.md`)
    rejected it as a completion artifact. The current repair chain closes
    post-rename pair drift, `ManifestCommitted` demotion, broken later-stage
    chain detection, recovery-primary validation, competitor journal adoption,
    the public D1 bypass (with architecture negative fixtures), and the Unix
    journal trust boundary (effective-user ownership, no group/other write),
    and owner-safe atomic quarantine for public entries plus transaction roots.
    All High audit findings are now closed. The per-call D1 fault seam also
    closes the initial journal-sync/temp-cleanup debt and covers final-manifest
    sync plus journal-chain cleanup failures. The literal persisted-state matrix
    now covers every durable row plus missing/corrupt/multiple-chain and
    competitor-entry catch-alls. All ten audit findings are repaired; focused,
    architecture, RFC, format, English-only, diff-hygiene, HIR,
    package-session, and all six long ownership serial gates are green. A fresh
    full sanitizer run on 2026-08-30 passed 308/308; the ownership event-overlay
    suite completed in 2212.13 seconds within its 3600-second budget. The D1
    repair candidate is verification-complete in the worktree and awaits landing
    with D5.
  - [~] D5 implementation candidate completed in the worktree:
    `ExecutableInspectionProfile` (format,
    x86-64/AArch64 machine, 64-bit width, sorted required runtime symbols, and
    the canonical runtime-reference domain) is
    bound into `VerifiedLinkPlan` and `LinkPlanId`; the live oracle is now 518
    bytes with `LinkPlanId` `54e60703...817c8dfd`. The bounded ELF64/Mach-O64
    inspector verifies format, machine, entry, and runtime symbols, rejects
    unresolved runtime references and malformed symbol names, and bisects
    format/bitness mismatch from structural corruption, and the
    consuming `linkAndPublish` operation drives D3/D4 -> inspection -> manifest
    -> internal D1. Tests cover Linux ELF publication, malformed output, machine
    mismatch, missing runtime symbols, and pure Mach-O inspection. KR5.3 remains
    CANDIDATE until this work lands; `zomc run` is technically unblocked in the
    same candidate series.
  - [~] KR5.4 implementation candidate covers the first supported host slice:
    `zomc run` reuses
    the package-only compilation path, lowers the scalar module initializer to
    a native object, adds the bundled Linux x86-64 `_start` object, builds a
    profile-bound `VerifiedLinkPlan`, runs `linkAndPublish`, applies
    `runCompatibility`, executes the published artifact through the shell-free
    subprocess primitive, and removes the transaction directory. The backend-on
    `native-execution-cli` drives the real installed-consumer package end to end
    and passes; the frontend-only lane remains fail-closed. KR5.4 remains
    CANDIDATE until the link/publication series lands.
- [ ] A3 drop materialization: add the deferred MIR drop statement [O5/KR5.5]
  - 2026-08-28 reassessed: the KR premise ("reachable now that a LIR/backend
    consumer exists") is not true on disk. Adding the MIR drop statement *kind*
    alone (enum variant + `OneOf` member + `encodeStatement` case 0x08 + byte
    oracle) is bounded, but it would be dead: drop elaboration
    (`compiler/ownership/drop-elaborated-mir.cc`) emits a separate
    `DropDischargeRecord` inventory consumed only in tests, and grep finds zero
    drop/cleanup consumers in `compiler/lir/**` or `compiler/backend/**`
    (`mir-to-lir.cc` admits only the constant-return scalar shape;
    `llvm-translator.cc` emits only a single integer `ret`). Materializing a drop
    end-to-end also needs ~30 positional MIR shape verifiers in `built-mir.cc` to
    admit a trailing drop before `Return`, plus a drop-aware LIR/backend path that
    does not exist. Blocked-with-cause: adding a fail-closed placeholder kind with
    no producer violates design principle #4. 2026-08-29 review (Codex + Claude)
    reconfirmed on disk: `MirStatementKind` still stops at
    `UnsafeScopeBoundary=0x07` (no Drop), `DropElaborator` still only builds the
    `DropDischargeRecord` inventory, and `compiler/lir/**` + `compiler/backend/**`
    still hold zero drop/cleanup/destructor consumers. Multi-block lowering
    landing does NOT unblock this: a multi-block consumer is not a drop-aware
    consumer. The real unblock needs four things together - map
    `DropDischargeRecord.event` to a verifiable MIR cleanup-insertion seam,
    define a LIR drop/destructor-call expression + runtime-symbol ABI, a backend
    that consumes and emits the call, and a shape-verifier migration from
    positional to structural admission of a verified cleanup. Sequenced after a
    drop-aware LIR/runtime consumer exists, not merely after KR5.2.

### O6 - advance the accepted governance surface (added 2026-08-28)
- [~] RFC 0022 flow-typing ACCEPTED -> IMPLEMENTING + first verified fact slice
  [O6/KR6.1]
  - [x] 2026-08-28 `b98922a7`: landed the RFC 0005 canonical union/intersection
    *constructor* (member canonicalization), the `join` primitive RFC 0022 flow
    refinement is defined on. Before this, `buildSet`/`buildOptional` in
    `compiler/checker/facts/signature-facts.cc` only hand-sorted/deduped members
    and relied on the canonical encoder, which is a *verifier* that rejects any
    non-canonical set - so source annotations `i32 | never`, `i32 & any`, and
    `i32 | i32` were compile errors, contradicting RFC 0005 L704-709. The new
    shared `canonicalizeSet` drops the identity element (`Never` in a union,
    `Any` in an intersection), collapses to the annihilator (`Any` in a union,
    `Never` in an intersection), sorts+dedups by canonical key bytes, and reduces
    empty->`Never`/`Any` and single->member. Members stay whole so the semantic
    and RFC 0015 key-pattern rails remain aligned. New
    `facts-union-canonicalization-test` (8/8) locks the RFC 0005 `i32 | null`
    golden vector (exact 35-byte key + SHA-256 `95145d7b...`) and the
    nested-union fail-closed boundary; facts-signature/dispatch, semantic-type-*,
    and marker-proof coherence tests remain green (no `TypeKeyPattern`
    regression).
  - [ ] Remaining before IMPLEMENTING can be earned honestly: the RFC 0005
    subtype `P <: D` relation and the disjoint relation (RFC 0022's `narrow`/
    `exclude` and the "effective type is a subtype of the declared type"
    invariant are defined on them; neither exists as a callable API yet), then
    nested same-kind flattening (needs a `TypeKeyPattern` flatten path), then the
    RFC 0022 ACCEPTED -> IMPLEMENTING transition and a first verified flow-fact
    slice. Blocked-with-cause: those relations are the next large RFC 0005
    workstream; the constructor above is the first landed piece of it, not the
    whole prerequisite.
  - 2026-08-28 subtype-relation reassessment: the RFC 0005 subtype relation is
    NOT a bounded leaf, because RFC 0005 discharges subtyping as an explicit
    recorded `CoercionAdjustment` at enumerated coercion sites, not a free
    `isSubtype(a,b)->bool`. The live consumer site
    (`compiler/checker/body/body-checker.cc:2837,2940`) relates initializer/return
    types to declared types by *exact equality* today (`!=` -> `rejectTypeMismatch`,
    always recording `noAdjustment`), so accepting a subtype means *producing* a
    coercion fact. But `compiler/hir/hir-module.cc:1437` `noUnsupportedFacts` is a
    hard gate that only lowers a module when `facts.coercions().size() == 0`, so a
    produced coercion is dead-on-arrival at the HIR boundary, and MIR has no
    coercion execution. A non-dead subtype slice (even just `Never <: T` via
    `NeverTo`, `T <: Any` via `ToAny`) must therefore change three fail-closed
    phases together - checker accept-site + HIR `noUnsupportedFacts`/lowering +
    MIR execution - which is a multi-phase pipeline workstream, not a bounded
    verifiable slice. Adding an `isSubtype`/coercion producer alone would be dead
    code (design principle #4), the same trap that correctly blocked KR5.5.
    Confirmed further: MIR->LIR (`compiler/lir/mir-to-lir.cc:30` `integerWidthFor`)
    lowers only fixed-width integer primitives; `any`, `never`, and unions are
    explicitly outside the lowerable subset, so a `ToAny`/`NeverTo` coercion has
    no runnable end-to-end path (checker->HIR->MIR->LIR->LLVM) at all - the same
    backend-floor situation as KR5.5, established by reading the code, not asserted.
  - 2026-08-28 nested-flatten + alias reassessment: the deferred nested-union
    flatten (`(A|B)|C` -> `A|B|C`) is a bounded leaf in isolation (plain canonical
    Union, no coercion), but its only source trigger today is literal
    parenthesization, because type-alias transparency is itself unbuilt:
    `buildNamedDefinition` (`signature-facts.cc:1420-1426`) accepts only
    Interface/Class/Struct/Enum/Error and returns none for a `TypeAlias`, so
    `type N = i32 | null; let x: N` fails closed and no alias expands to a union.
    Flattening therefore has no natural (non-contrived) consumer until alias
    transparency lands, and it would still need a new `TypeKeyPattern` child
    accessor on the coherence-critical pattern rail. Both alias transparency and
    the subtype pipeline remain the real KR6.1 blockers; neither is a bounded
    no-integration slice.
- [x] RFC 0044 formatter REVIEW -> ACCEPTED, then Doc-IR core + `zomc fmt`
  [O6/KR6.2] (backend-independent)
  - [x] 2026-08-28: RFC 0044 REVIEW -> ACCEPTED. Five-owner review after the RFC
    0023 lossless-snapshot dependency reached ACCEPTED (Acceptance Criterion #1);
    verified the input contract (RFC 0023 recoverable CST), no new diagnostic
    code, the intact Doc-IR engine + pinned width, and eight prior-art formatters.
  - [x] 2026-08-28: RFC 0044 ACCEPTED -> IMPLEMENTING with the pure Doc-IR core
    landed as evidence (Implementation Plan step 2). `compiler/format/doc.{h,cc}`
    defines the closed Wadler/Lindig `Doc` constructor set (text/concat/line/
    softline/hardline/group/indent/ifBreak/fill); `compiler/format/doc-renderer.
    {h,cc}` implements the total search-free width-driven layout pass at the
    pinned 100-column width. `doc-renderer-oracle-test` asserts flat-vs-broken
    group decisions, hardline forcing, nested indent accumulation, ifBreak
    selection, fill packing, and determinism (8/8 pass under the frontend
    sanitizer build; no lexer/parser/CST/filesystem dependency). check-rfc passes.
  - [x] 2026-08-28: canonical edit set landed (Implementation Plan step 3, pure
    data). `compiler/format/source-edits.{h,cc}` defines `SourceReplacement`
    (half-open byte range + replacement UTF-8 text, validating factory) and
    `FormatResult` (`Unchanged`/`Edits`/`Rejected`) whose `normalize` sorts by
    start, rejects overlap (no edit prefix), and merges adjacent replacements,
    plus `apply(source)` that splices the sorted disjoint edits. New
    `source-edits-test` covers inverted-range rejection, empty/no-op ->
    Unchanged, overlap -> Rejected, sort+merge, and apply (5/5 pass; no
    CST/printer/filesystem).
  - [x] 2026-08-28 `2bb026b8`: the token-preserving printer over the RFC 0023
    lexeme stream landed - `compiler/format/lexeme-printer.{h,cc}` composes the
    three prior pieces (the live-lexer lexeme stream `c6df8569`, the Doc-IR core,
    and its renderer) into the first working formatter path.
    `formatLexemeStream` emits one `Doc::text` per lexeme spelling (verbatim,
    never re-lexed) and renders it, producing byte-identical output (the
    token-preserving identity baseline RFC 0044 L110/L125 mandate), and
    `tokenSequencePreserved` is the independent token/trivia verifier (RFC 0044
    L263). Tests drive the real lexer and assert byte-exact reproduction across
    whitespace/comment/leading-trailing/no-trivia sources plus idempotence and
    token preservation (5/5). This is the safe base; whitespace-normalizing reflow
    at trivia boundaries, range formatting, atomic writes, and `zomc fmt` remain.
  - [x] 2026-08-28 `7bfedff0`: the first real (non-identity) reformat landed -
    `normalizeTriviaWhitespace` in `compiler/format/lexeme-printer.{h,cc}` computes
    a `FormatResult` edit set purely from the trivia stream: strip horizontal
    whitespace before each line break, and canonicalize the file-final whitespace
    to exactly one trailing newline (RFC 0044 fixed style + L127-128, the
    parser-free whitespace normalizations). Edits touch only `Whitespace` trivia
    bytes, so the output re-lexes to the identical token sequence (proved via
    `tokenSequencePreserved`); an already-canonical source yields `Unchanged`.
    Tests: strip, add-final-newline, collapse-blank-lines, Unchanged, and
    token-preservation (printer test file 10/10).
  - [x] 2026-08-28 `48394183`: the RFC 0044 fixed-style "one space after a comma"
    rule added to `normalizeTriviaWhitespace` - a parser-free, same-line
    normalization (a comma is an unambiguous token). For each Comma token followed
    by a same-line separator it collapses a following no-line-break Whitespace
    lexeme to one space and inserts one space when the comma abuts the next token;
    a comma-before-newline (multiline list) is left to structural reflow.
    `tokenSequencePreserved` refined to compare only significant Token lexemes
    (the correct RFC 0044 guarantee: trivia may change, tokens are preserved), and
    to reject inserted/dropped tokens. Tests: `a,b`->`a, b`, `a,   b`->`a, b`,
    comma-before-newline unchanged, and token preservation across `f(a,b,c)`
    (printer test file 14/14).
  - [x] 2026-08-29 `3be6db99`: `zomc fmt` / `zomc fmt --check` landed, closing the
    KR6.2 criterion (REVIEW -> ACCEPTED, Doc-IR core, and the two CLI verbs). The
    `fmt` subcommand in `utils/zomc/zomc.cc` reads each source, lexes it, builds the
    RFC 0023 verified lexeme stream (`buildLexemeStreamFromTokens`), computes the
    canonical edit set (`normalizeTriviaWhitespace`), and applies it in place;
    `fmt --check` reports drift with a non-zero exit and writes nothing. New
    `format-cli` CTest gate (`tests/tools/check-fmt-cli.py`) drives the real zomc
    binary: `--check` rejects a drifted file and leaves it untouched, accepts a
    canonical file, `fmt` rewrites drift to canonical form, and `fmt` is idempotent
    (passes). C++ format and check-rfc pass.
  - [ ] LATER (beyond KR6.2): syntax-directed reflow over the CST and range
    formatting. The KR6.2 criterion (REVIEW -> ACCEPTED, Doc-IR core, `zomc fmt` /
    `zomc fmt --check`) is met by the bullets above. What remains is a later RFC
    0044 refinement, not part of this KR: reflow that normalizes whitespace at
    trivia boundaries only where the result re-lexes identically (RFC 0044
    L127-128), driven by the parser's structural grouping. That grouping is coupled
    to the RFC 0023 parser cutover (slice #3, the `ParseSyntaxVerifier` ->
    `ast::Tree` replacement across `compiler/lexer|parser|ast`); the Doc-IR core,
    the canonical edit set, the lexeme stream, the token-preserving printer, the
    parser-free whitespace normalizations, and the CLI are all landed.
  - 2026-08-28 `ed8a797f`: the shared RFC 0023 lossless-CST prerequisite advanced
    - the recoverable lexeme partition codec + verifier landed and RFC 0023 is now
    IMPLEMENTING (see KR6.3). The `fmt` path now feeds the formatter from the live
    lexer's tokens through `buildLexemeStreamFromTokens`; the deeper RFC 0023
    parser cutover (building the `RecoverableSyntaxTree`, slice #3) is only needed
    by the later syntax-directed reflow, not by the shipped `zomc fmt`.
- [~] RFC 0023 IDE/LSP first rollout slice (semantic-snapshot facade) [O6/KR6.3]
  - [x] 2026-08-28 `ed8a797f`: RFC 0023 ACCEPTED -> IMPLEMENTING with a landed
    pure-data first slice as evidence, correcting the earlier "no bounded slice
    exists" reading. RFC 0023's recoverable CST is built from one closed lexeme
    stream whose normative foundation (L484-551) is pure data with an exact
    partition invariant, independent of the live lexer/parser - the same shape as
    the RFC 0043 link-plan codec. `compiler/cst/lexeme-codec.{h,cc}` lands the
    closed `CstLexeme` algebra (Token/Trivia/Invalid), the domain-separated
    `zom.cst-lexemes` codec + `LexemeStreamId`, and the independent
    `LexemePartitionVerifier` (exact adjacent partition of `[0, sourceByteCount)`,
    spelling width, non-empty partition, content-digest reconstruction). Frozen
    188-byte oracle + fail-closed matrix, 10/10; frontend-only, no live
    lexer/parser change. approvers already cover the eight required owners.
  - [x] 2026-08-28 `69292ead`: second pure-data slice landed - the recovery
    element sequence codec + verifier. `compiler/cst/recovery-codec.{h,cc}` adds
    the closed `RecoveryElement` algebra (MissingToken/MissingSubtree/
    SkippedTokens) with validating factories, the canonical recovery order
    (`compareCanonical`), the domain-separated `zom.cst-recovery` codec +
    `RecoverySequenceId`, and the independent `RecoverySequenceVerifier` binding a
    sequence to a verified lexeme stream and enforcing the RFC 0023 L541-551
    invariants (contiguous non-empty skip runs containing a significant lexeme
    with a covering range, zero-width in-range anchors, strict canonical order, no
    duplicate elements). Frozen 128-byte oracle + fail-closed matrix, 10/10;
    frontend-only.
  - [x] 2026-08-28 `c6df8569`: first live-producer bridge landed -
    `compiler/cst/lexeme-stream-builder.{h,cc}` `buildLexemeStreamFromTokens` turns
    the real lexer's token output over a source buffer into a verified lexeme
    stream. It walks the significant tokens, computes each byte range from its
    SourceLoc against the buffer base, fills every inter-token and trailing byte
    gap with a Trivia lexeme, skips the zero-width EOF token, and verifies the
    result through `LexemePartitionVerifier`. Integration tests drive the actual
    lexer over whitespace, comment, leading/trailing-whitespace, and no-trivia
    sources and assert byte-for-byte source reconstruction (4/4). This is the
    first RFC 0023 step with a live producer, not a hand-built fixture.
  - [x] 2026-08-28 `b0200614`: the bridge now splits each inter-token gap into
    precise RFC 0023 trivia sub-kinds - Whitespace (maximal whitespace run),
    LineComment (`//` to just before the line break), and BlockComment
    (`/* ... */`) - mirroring the lexer's own trivia scanning, so the trivia
    distinctions RFC 0023's test plan exercises are real. Reconstruction and the
    token-preserving printer are unaffected (spellings unchanged); block-comment
    and line-comment classification tests added (bridge test 6/6).
  - [x] 2026-08-28 `7c438f0d`: the `ParseSyntaxVerifier` acceptance predicate
    landed - `compiler/cst/parse-eligibility.{h,cc}` `parseEligibility` decides,
    from the verified lexeme stream and recovery sequence, whether a parse is
    eligible for AST construction: recovery empty, no `Invalid` lexeme, no
    error-severity parser diagnostic (RFC 0023 L561-563 conditions 1-3; condition
    4, byte coverage + digest, is already discharged by the `VerifiedLexemeStream`).
    Returns the closed `ParseEligibility` (`AcceptClean` or the first violated
    reason, incl. a recovery/stream-id mismatch guard). Tests 5/5. This composes
    the two prior CST models into the gate that fronts AST construction.
  - [x] 2026-08-30: reconciled the RFC 0023 implementation tracker to the live
    production evidence through `7c438f0d`. The tracker now records the lexeme
    partition, recovery sequence, live lexer bridge, precise trivia
    classification, and parse-eligibility slices separately, and names the next
    direct replacement as the parser event stream + `RecoverableSyntaxTree`,
    followed by recovery-free verified `ast::Tree` construction. No RFC contract
    or status changed.
  - [~] 2026-08-30 worktree candidate: the production parser no longer constructs
    `ast::Tree` directly. `ParserSyntaxFactory` records node/list/intern/root
    construction events through `ParserEventBuilder`; the buffered live lexer
    output becomes the verified byte-covering lexeme stream; the clean path binds
    the verified empty recovery sequence in `RecoverableSyntaxTree`.
    `ParseSyntaxVerifier` independently replays the event algebra, checks event
    identities and the RFC 0002 schema, and is the sole compiler AST construction
    path. Parser unit tests, event mutation tests, and all 918 AST conformance
    cases pass. Explicit recovery-element production and IDE-facing publication
    remain open, so KR6.3 stays PARTIAL/CANDIDATE.
  - [~] 2026-08-30 malformed-source candidate: live recovery frames now produce
    SkippedTokens or MissingSubtree records, retained parser diagnostics produce
    MissingToken/MissingSubtree, and Unknown or uncovered invalid bytes remain
    Invalid lexemes. Recovery is sorted, deduplicated, and checked by
    `RecoverySequenceVerifier`. `Parser::takeRecoverableSyntax()` publishes the
    immutable result once after either a clean or failed traversal, while
    `Parser::parse()` still publishes no compiler AST on recovery, Invalid
    lexemes, or parser errors. Exact ParserDiagnosticFact binding for Invalid
    lexemes and IDE query/snapshot publication remain open.
  - [ ] Remaining (the large integration, still pending): the parser event
    stream, the `RecoverableSyntaxTree`, wiring the recovery-element storage into
    a live parse, and the second half of `ParseSyntaxVerifier` - constructing the
    `ast::Tree` from a clean stream and verifying it against the RFC 0002 schema
    (condition 5) - i.e. the `ast::Tree` replacement across
    `compiler/lexer|parser|ast` (RFC 0023 slice #3, a direct production-boundary
    replacement), then document versions (#4), the editor semantic facade (#10),
    and the LSP adapter (#11). The lexeme-partition data model, the recovery
    codec, and now the live lexer->lexeme-stream bridge are in place; the parser
    cutover is the next large
    slice.
- [x] RFC 0045 debugger REVIEW -> ACCEPTED [O6/KR6.4] (real DAP post-KR5.4)
  - [x] 2026-08-28: seven-owner review; RFC 0043 dependency cleared (ACCEPTED),
    verified RFC 0045 layers on RFC 0043's executable artifact + `.zom-artifact`
    manifest and its own `.zom-debug`, invents no diagnostic code, cites five
    prior-art designs, Open Questions None. REVIEW -> ACCEPTED; implementation
    stays TBD (gated on native output + linking). KR6.4 COMPLETE.

## Progress log

- 2026-08-25 `2ed20c94` - G1: primitive arithmetic and bitwise binary operations
  lower end-to-end (new `MirRvalueKind::Arithmetic` 0x04 + operator enum, checker
  fact, HIR node, byte oracle).
- 2026-08-25 `3f7f5189` - G2 slice 1: N sequential `let` locals with
  scalar-literal / aggregate / identifier initializers; fixed two-local body
  shape generalized across admission, HIR, and MIR.
- 2026-08-25 `76eaa630` - G2 slice 2: a primitive binary operation is admitted
  as a local initializer, so `let x = a + b; let y = x * b; return y;` lowers
  end-to-end. G2 complete.
- 2026-08-25 `941faa2c` - G3: per-owner review guide added to the RFC 0016
  tracker. No approval or status transition recorded; RFC proposal bytes
  unchanged.
- 2026-08-25 Stretch retired after a read-only scope investigation. The item's
  premise ("no error-union semantic type exists; add one") was wrong on two
  counts: (a) the semantic error-union representation already exists and is
  implemented as `ErrorUnionShapeFact` over a canonical `Union` result (RFC
  0005), and (b) a standalone error-union `TypeData` variant is forbidden by RFC
  0005's frozen 15-variant algebra, so adding one would be an unauthorized
  contract, not a reachable slice. No code change; the plan text was corrected
  to match the real model. What is genuinely open (error-operator lowering)
  stays backend/governance-blocked, unchanged from the Out-of-scope section.
- 2026-08-26 `26a30ada` - G3: RFC 0016 accepted. A real per-owner review of the
  `cfabf1a0` snapshot returned 8/8 approve after two blocking objections were
  resolved and re-verified: the `.agents`->`.codex` routing correction
  (task-router) and the repository-wide `zom-v1`->`zom` codec-fixture
  regeneration across RFC 0016 and RFC 0010 (ir-backend), each checked against
  the live encoder and the live target-registry test. Accepted snapshot
  `ec27f6d3`; approvers filled, decision recorded. No approval fabricated.
- 2026-08-26 `a6cdee95` - G4: RFC 0021 moved DRAFT->REVIEW after its stated RFC
  0016 dependency gate cleared, with all nine bound upstream snapshot pins
  resynchronized to current authoritative hashes. Required-owner review is now
  pending (owner work).
- 2026-08-26 - G6 re-scoped after an end-to-end probe: the monolith is not a
  bounded fixed-shape generalization (the mut-local write value is
  scalar-literal-only across six interlocking HIR count formulas and ~8
  hand-specialized MIR write functions). Split into G6a-1 / G6a-2 / G6b; the
  empty-loop-body and scalar-literal-write-RHS rejections stay fail-closed.
- 2026-08-26 - G6a-1 landed: a mutable-local write with a parameter-reference
  RHS lowers end-to-end (`fun f(a: T) -> T { mut x = <lit>; x = a; return x; }`).
  A per-write literal-XOR-parameter value descriptor threads through the HIR
  count formulas so an all-literal body keeps byte-identical ids; MIR declares
  the parameter local and lowers the overwrite to a place-use. No new codec
  byte; the all-literal write regression guard proves no id shift.
- 2026-08-26 - G7 landed: RFC 0021 ACCEPTED on a real ten-owner review; one
  mechanical codec-oracle label defect (59 -> 56 bytes) was found and fixed
  in-pass. No IMPLEMENTING pointer; the LLVM 22.1.8 gate still precedes any
  backend code.
- 2026-08-26 - Q4 reframed as OKRs (O1 subset-to-MIR, O2 backend gates
  [aggressive], O3 governance/spec) after G1-G4 plus G5/G6a-1/G7 landed. The
  aggressive O2 target (first MIR -> LIR -> LLVM path) is encoded as conditional
  KRs gated on the LLVM 22.1.8 toolchain; they will be reported blocked, not
  worked around, if the environment cannot provide it.
- 2026-08-27 - KR1.1 (G6a-2) landed: a mutable-local write RHS may be a primitive
  binary operation (`mut x = 0; x = a + b; return x;`). The binary write value is
  a `HirPrimitiveBinaryExpression`; its two operand nodes are allocated in a
  trailing id region after the return value, so literal/reference write ids stay
  byte-identical (regression guards prove it). MIR lowers to an
  Arithmetic/Comparison rvalue over parameter place-uses / literal constants. No
  new codec byte.
- 2026-08-27 - KR3.3 verdict recorded (DROP; error-operator fact spec-blocked on
  RFC 0006, code-traced), KR3.1 RFC 0022 held in REVIEW on a genuine
  ZOM4096-4098 diagnostic-code collision, KR3.2 RFC 0043 held in DRAFT on three
  unresolved before-REVIEW Open Questions (trackers committed `1ca50a55`). KR2.1
  (G8) assessed: the 22.1.8 pin is a real upstream tag; split into KR2.1a
  (in-repo fail-closed CMake gate, reachable) and KR2.1b (install 22.1.8, blocked
  on external provisioning). No transition or approval fabricated.
- 2026-08-27 - KR1.4 landed (`1685b512`): lowerable-constructs inventory, 22
  constructs at HEAD vs the 6-family 2026-08-25 baseline (+6 net-new). Surfaced
  that call admission out-runs proven lowering (feeds KR1.3).
- 2026-08-27 - KR1.2 (G6b) landed: a `while` whose non-empty body writes mutable
  locals (`while (cond) { x = <lit|param|binary>; }`) lowers to a reducible
  four-block CFG (bb1 init+Goto, bb2 SwitchInt header, bb3 body writes + back-edge
  Goto, bb4 Return). `HirLoopStatement` gained a `body` node-id list (empty for
  the empty-body loop, which stays byte-identical). No new codec byte. This
  completes G6 (all of G6a-1/G6a-2/G6b landed).
- 2026-08-27 `7564b86a` - KR1.3 (RFC 0009 call widening) landed: a same-module
  direct call with N arguments lowers to a MIR Call with N operands. The call
  machinery was already N-ary; the real blocker was a single-parameter-fixed
  parameter-return function builder/verifier (a multi-parameter callee failed
  MIR publication with ZOM9944). Generalized to N parameters; single-parameter
  case byte-identical. No new codec byte. O1 is now complete. The
  lowerable-constructs inventory was refreshed to mark the call and loop-body
  gaps closed.
- 2026-08-27 - KR2.1a re-assessed and corrected: an earlier note called the
  in-repo CMake gate separately reachable. Reading the accepted RFC 0016 CMake
  contract showed it is an unconditional exact-22.1.8 configure gate whose
  positive fixture needs a real 22.1.8; landing it in a 19.1.5-only env would
  break the frontend build (which links no LLVM today) or be drift/dead code.
  KR2.1a collapses into KR2.1b (22.1.8 provisioning), so all of O2 is blocked on
  provisioning I cannot perform in-session. Recorded rather than worked around;
  no ambient LLVM admitted, no CMake change forced to look productive.
- 2026-08-27 `a081de02` - KR3.1 complete: RFC 0022 ACCEPTED after fixing the two
  real defects the first review caught (ZOM4096-4098 code collision re-allocated
  to the free ZOM4100-4102; producer-tag corrected to FlowRefinement=0x14). Nine
  owners approved the corrected snapshot; implementation stays TBD.
- 2026-08-27 `55d2b60b` - KR3.2 complete: RFC 0043 advanced DRAFT -> REVIEW by
  authoring its three deferred design decisions from mature prior art (a
  ToolchainClosure discovery record, an RFC 0010 failure-algebra extension, a
  native-vs-cross CI lane matrix). approvers empty; no ACCEPTED fabricated. O3 is
  complete. O1 was already complete; O2 remains blocked solely on LLVM 22.1.8
  provisioning, which cannot be performed in-session and will not be faked.
- 2026-08-27 - KR2.1b provisioning ATTEMPTED for real (not assumed). Obtained
  genuine LLVM 22.1.8 from apt.llvm.org noble (commit `ca7933e47d3a`), extracted
  into `$HOME/toolchains/llvm22` without root; `llvm-config --version` reports
  `22.1.8`, X86+AArch64 present, all 11 RFC-0016 components resolve. But the apt
  build requires GLIBC_2.38 and this host is Debian glibc 2.36: `llc` fails with
  `version 'GLIBC_2.38' not found` and static linking fails on undefined
  `__isoc23_strtol/strtoll/strtoull` (glibc-2.38 C23 variants, absent from the
  host libc). The glibc-2.36-compatible GitHub official tarball is
  network-unreachable (release CDN returns 302 then 0 bytes). This is a hard
  host-ABI floor. Recorded honestly; O2 stays blocked. It also empirically
  justifies RFC 0016's mandated ubuntu-24.04 (glibc 2.39) CI runner. No ambient
  LLVM admitted, nothing faked.

- 2026-08-27 `db8fe17e` - KR2.1a: the RFC 0016 fail-closed LLVM discovery gate
  landed as cmake/utils/llvm.cmake behind ZOM_ENABLE_LLVM_BACKEND (default OFF),
  with the positive fixture and 13 negatives (each asserting its exact
  ZOM-CMAKE-LLVM-* identifier) passing. KR2.1b was completed the same day by
  building LLVM 22.1.8 from source on this glibc-2.36 host (the apt.llvm.org
  prebuilt needed glibc 2.38); llvm-config 22.1.8, X86+AArch64, llc runs, all 11
  components, verifyModule links+runs. KR2.1 DONE.
- 2026-08-27 `cb8e1e3b`,`f9c670bf` - KR2.2: RFC 0016 and RFC 0021 transitioned
  ACCEPTED -> IMPLEMENTING with implementation pointers set; the RFC 0016 first
  slice (the CMake gate) is the landed evidence, and both RFC-0021 preconditions
  (0016 IMPLEMENTING + provisioned 22.1.8) were cleared. Real transitions, no
  fabrication.
- 2026-08-27 `fabf520d` - KR2.3: first authorized RFC 0021 slice landed - the LIR
  identity/carrier/layout/ABI/runtime-symbol/source-location/revision codec
  foundation (pure data, no MIR consumer, no LLVM linkage). The live
  LirAlgebraCodec reproduces the RFC's documented 56-byte empty-registry oracle
  and SHA-256 03106c34...; oracle, mutation, and fail-closed tests pass. Only
  KR2.4 (minimal MIR -> LIR -> LLVM IR + verifyModule) remains in O2.

- 2026-08-27 `4f078d78` - KR2.4: minimal MIR -> LIR -> LLVM IR path landed. A
  scalar module initializer lowers through a minimal LIR model + lowerToLir into
  a new compiler/backend/llvm/ Pimpl shim (the sole llvm/-including TU) and passes
  llvm::verifyModule, emitting `ret i32 42`. Gated behind ZOM_ENABLE_LLVM_BACKEND
  (OFF = frontend unaffected). Verified: OFF sanitizer build 142/142 clean; ON
  build gate passes against provisioned 22.1.8; llvm-translation-test 2/2. The
  then-current O1-O3 aggressive targets were complete; later scope added O4-O6.

- 2026-08-27 `e78cc98e` - KR2.5: LLVM 22.1.8 made a self-managed,
  commit-reproducible dependency (strategy B). third_party/llvm/llvm-lock.json
  locks tag+commit ca7933e47d3a as the source of truth; scripts/provision-llvm.py
  idempotently builds it from source; scripts/check-llvm-lock.py fails closed on
  lock<->CMake-gate drift; a check-backend-llvm CI job (ubuntu-24.04, cache keyed
  on the commit) runs the discovery-gate fixtures + llvm-translation-test with the
  backend ON while frontend jobs stay OFF. Locally verified (provisioner
  idempotent-skip + reject, lock consistency, yaml parse); the CI job is authored
  and logic-aligned but NOT yet run - GitHub Actions is green-verifiable only
  after push, and no passing run is claimed. A macOS backend lane is outside the
  current Linux backend slice.

- 2026-08-27 - Q4 scope expanded (user goal: polish the compiler to a complete,
  usable toolchain) with a new Objective O4: end-to-end run, LSP, package
  manager, build tool, formatter, debugger. Each KR is classified against on-disk
  reality: RFC 0043 (run/link) and 0023 (LSP) are REVIEW, 0044 (formatter) and
  0045 (debugger) are DRAFT, 0012 (package) is IMPLEMENTING with live
  driver/package code. Per this plan's governance rule, governance-blocked
  capabilities are written as "advance the RFC / land the reachable slice", not
  "ship the feature". O4 is explicitly a multi-quarter program; Q4 commits the
  honest first step of each and schedules the rest into 2027 Q1. No feature is
  reported done without being built and verified, and no RFC transition is
  fabricated.

## Status against the OKRs (updated 2026-08-30)

Q4 has not started. The current state is a pre-quarter implementation lead, not
a quarter-close result. Strict grading counts the exact KR deliverable and
distinguishes committed evidence from a verified worktree candidate.

| Objective | Complete | Partial or candidate | Blocked | Current result |
|---|---:|---:|---:|---|
| O1 - subset to Built MIR | 4/4 | 0 | 0 | Complete |
| O2 - backend gates and first native output | 4/5 | 1 | 0 | KR2.5 awaits remote backend CI evidence |
| O3 - governance and reachability decisions | 3/3 | 0 | 0 | Complete |
| O4 - usable-toolchain first slices | 4/6 | 2 | 0 | IDE facade partial; `zomc build` is a worktree candidate |
| O5 - runnable native path | 2/5 | 2 | 1 | Link/run candidates verified in the worktree; drop blocked |
| O6 - accepted-governance rollout | 2/4 | 2 | 0 | Formatter/debugger governance complete; flow and IDE partial |
| **Total** | **19/27** | **7/27** | **1/27** | **70% committed complete** |

### Completed objectives and KRs

- **O1 is complete.** The admitted frontend-to-Built-MIR subset includes the
  mutable-write binary RHS, non-empty loop body, N-argument same-module direct
  call, and the evidence-backed lowerable-construct inventory.
- **O2's compiler work is complete through KR2.4.** LLVM 22.1.8 discovery, the
  self-managed source lock/provisioner, LIR foundations, verified LLVM
  translation, and object emission exist. KR2.5 remains partial because the
  backend CI job has not produced a remote passing run.
- **O3 is complete.** RFC 0022 reached ACCEPTED, RFC 0043 reached REVIEW and later
  IMPLEMENTING, and KR3.3 produced a code-traced DROP verdict instead of an
  unreachable checker-only implementation.
- **O4 complete KRs are KR4.1, KR4.3, KR4.5, and KR4.6.** Object emission and the
  RFC 0043 governance gate, the RFC 0012 package slice, formatter governance, and
  debugger governance all satisfy their exact O4 deliverables.
- **O5 complete KRs are KR5.1 and KR5.2.** The CLI emits a native object, and the
  backend library lowers the admitted conditional, loop, comparison, parameter-
  arm, and same-module-call shapes under backend tests. Production CLI selection
  remains the scalar module-initializer slice.
- **O6 complete KRs are KR6.2 and KR6.4.** `zomc fmt` / `zomc fmt --check` and the
  formatter core are implemented; RFC 0045 is ACCEPTED. Debug-adapter code is not
  part of KR6.4.

### Partial and candidate KRs

- **KR2.5 - Partial.** The exact LLVM source lock, provisioner, local gate, and CI
  job definition are committed. Completion requires a passing remote backend CI
  run; authored workflow text is not execution evidence.
- **KR4.2 - Partial.** RFC 0023 is IMPLEMENTING and CST foundations are landed,
  but no versioned IDE semantic facade or document-version/cancellation core is
  production-wired.
- **KR4.4 - Candidate.** `zomc build` now drives a multi-module package through
  verified Built MIR with lock update/replay coverage and an output-free command
  surface. Sanitizer and LLVM integration tests pass, and the full sanitizer
  suite is 308/308; completion requires the candidate to be committed and
  integrated.
- **KR5.3 - committed and pushed; open on the owner re-approval decision.** D5
  executable inspection, consuming `linkAndPublish`, and all D1 audit repairs are
  committed and pushed to `develop` (through `0286b894`). Focused sanitizer and
  LLVM tests plus architecture, RFC, format, English-only, and hygiene gates pass;
  the full sanitizer suite is 308/308. RFC 0043 is at hash `c2e366c8`; six
  subagent gates returned RECOMMEND, recorded as Re-Approval Evidence in the
  tracker. KR5.3 is not complete until the delegated owner re-approval decision is
  recorded against `c2e366c8`.
- **KR5.4 - Candidate.** The current worktree runs the Linux x86-64 scalar package
  through compile, object emission, link, inspection, publication, compatibility
  checking, and execution. `native-execution-cli` passes with the LLVM backend,
  but the slice shares KR5.3's uncommitted candidate boundary.
- **KR6.1 - Partial.** Union/intersection canonicalization landed, but RFC 0022
  remains ACCEPTED with no implementation pointer. Subtyping requires a real
  coercion producer plus HIR/MIR consumers; a free boolean relation would be dead
  code.
- **KR6.3 - Partial with parser candidate.** Lexeme and recovery codecs, the live
  lexer bridge, trivia classification, and parse eligibility are landed. The
  worktree candidate adds the clean production parser event stream,
  `RecoverableSyntaxTree`, and independently replayed verified AST bridge.
  The candidate also produces live MissingToken/MissingSubtree/SkippedTokens,
  retains Invalid bytes, and exposes a single-use parser recoverable result.
  Exact diagnostic binding, IDE query/snapshot publication, document versions,
  semantic facade, and LSP adapter remain open.

### Blocked KR

- **KR5.5 - Blocked.** Ownership analysis produces drop discharge records, but
  Built MIR has no produced Drop statement and LIR/runtime have no drop-aware
  consumer. The KR remains blocked until cleanup insertion, structural MIR
  verification, a LIR destructor/drop operation, and its runtime ABI can land as
  one non-dead production path.

### Q4 close requirements

The quarter cannot close from checklist state alone. It requires:

1. every Complete KR to name a committed producer, verifier/consumer boundary,
   and project-native test;
2. KR4.4/KR5.3/KR5.4 either landed with the full verification matrix or left
   Candidate;
3. the `zomc build` candidate committed before KR4.4 becomes Complete;
4. a versioned semantic-snapshot facade before KR4.2/KR6.3 becomes Complete;
5. remote backend CI evidence for KR2.5;
6. blocked KRs to retain their exact dependency and publish no placeholder; and
7. live design notes and RFC trackers to match production code at quarter close.

## Follow-up work (beyond this quarter's committed goals)

*Refreshed 2026-08-28. The backend-cutover and accepted-governance threads that
were listed here have been promoted into committed OKRs **O5** (runnable `zomc
run`) and **O6** (advance the accepted governance surface) in the Q4 OKRs
section above; see those for the authoritative KR list and grading. This section
now records only work that remains genuinely beyond O5/O6 and its blocking
cause. RFC statuses are live as of 2026-08-29:
0006/0009/0010/0016/0021/0023/0043/0044 IMPLEMENTING, 0022/0045 ACCEPTED,
0046 DRAFT.*

### Still beyond the committed OKRs (blocked, cause recorded)
- **Mutable-write value beyond scalar-literal (frontend).** Per
  [[g6-blocked-hir-fixed-id-digest]], the mut-write value is scalar-literal-only
  across six interlocking HIR count formulas and ~8 hand-specialized MIR
  functions; generalizing it needs the HIR fixed-id digest restructuring, a large
  refactor rather than a bounded slice. Prerequisite for wider O5/KR5.2 shapes.
- **Method-receiver and generic-call lowering (frontend).** KR1.3 landed
  multi-argument direct calls; method dispatch and generic instantiation to Built
  MIR remain, feeding O5/KR5.2.
- **Error-operator fact slice.** Spec-blocked on RFC 0006 (no error-union value
  type; raising calls form no callable shape) and RFC 0046 (DRAFT); the
  checker-only probe was DROP-verdicted in KR3.3. Revisit only after RFC 0006
  lands an error-union representation.

## RFC 0043 status snapshot from locally reproducible evidence (2026-08-31)

Recorded from the current build environment only; it does not vouch for
LLVM-backend execution evidence produced elsewhere in this plan.

- RFC 0043 landed on `develop` as reviewed commits `a737dda5` (D5 executable
  inspection + `linkAndPublish`), `89816cb3` (D1 publication-recovery
  hardening), `b2310198` (`zomc run` consumer + Linux x86-64 runtime entry
  wiring), and `c7e842f0` (`zomc build` subcommand + shared option groups),
  after `7e047a32` (latent publish-test use-after-free fix).
- Reproduced with `ZOM_ENABLE_LLVM_BACKEND=OFF` (default sanitizer preset): the
  D5/D1/inspector/link-plan-codec unit tests are green and `native-execution-cli`
  asserts the fail-closed unavailable state.
- **KR5.4 real `zomc run` execution is deferred to an LLVM-22 environment.** The
  execution path needs an LLVM backend build pinned to LLVM 22.1.8, which is not
  available here, so the `llvm` preset cannot be configured in this environment.
  The fail-closed guard is not execution success; no all-platform completion is
  claimed.
- Open: Mach-O entry-symbol mangling (`_zom` vs `zom`); execution stays Linux
  x86-64 only.
