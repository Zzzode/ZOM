let a = `hello`;
let b = `hello ${name}`;
let c = `a ${x} b ${y} c`;
let d = `trailing dollar $`;

fun close_global_scope() {
    return;
}

let e = `after global brace ${close_global_scope()}`;
let f = `nested brace ${{value: name}.value}`;
