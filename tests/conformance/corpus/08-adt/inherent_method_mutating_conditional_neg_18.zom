struct Cell {
  value: i32,
  mutating fun choose(this, flag: bool) -> i32 {
    if (flag) { return 42; } else { return 0; }
  }
}
fun entry() -> i32 {
  mut cell = Cell { value: 0 };
  return cell.choose(true);
}
