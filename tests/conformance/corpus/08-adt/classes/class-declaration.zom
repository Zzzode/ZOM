class Animal {
  public let name: str;

  init(this, n: str) {
    this.name = n;
  }

  public fun makeSound(this) -> str {
    return "noise";
  }
}

class Dog : Animal {
  private let breed: str;

  init(this, n: str, b: str) {
    super.init(n);
    this.breed = b;
  }

  public fun makeSound(this) -> str {
    return "Woof";
  }
}
