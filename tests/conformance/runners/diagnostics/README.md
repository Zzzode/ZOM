# Diagnostics Conformance Runner

Diagnostics conformance will consume `conformance/corpus` sources and
`conformance/expectations/diagnostics` oracle files. No CTest target is
registered until the runner has executable semantics.
