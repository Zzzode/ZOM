section: "05-statements borrow diagnostics"
covers_rule: "matchStatement"
expected: "ACCEPT"
notes:
  - "Syntax is valid; diagnostics runner checks that a borrow scoped to a match arm no longer blocks a move after the match."
