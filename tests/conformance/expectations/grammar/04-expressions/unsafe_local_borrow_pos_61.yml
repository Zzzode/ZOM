section: "§4.6 Unsafe block expression containing a local borrow"
covers_rule: "unsafeBlockExpr, unaryExpr (exprReference)"
expected: "ACCEPT"
notes:
  - "Syntax admits an unsafe block whose expression borrows a local binding; ownership validation is a later compiler phase."
