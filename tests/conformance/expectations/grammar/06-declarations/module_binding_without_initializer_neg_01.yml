section: "§6.9 LetDecl, ConstDecl"
covers_rule: "letDecl (initializer optional)"
expected: "ACCEPT"
notes:
  - "The parser admits a `let` with no initializer; the checker refuses the form until definite assignment lands."
