// RUN: cd %corpus && ! %zomc compile --check 06-declarations/const_without_initializer_neg_01.zom > %t 2>&1
// RUN: %FileCheck %s --input-file %t

// A `const` declaration is a named compile-time value and must carry an
// initializer. The parser enforces the grammar requirement, so the form is a
// parse error, never an internal compiler error.
// CHECK: Error [ZOM2106]: `const` declaration requires an initializer
