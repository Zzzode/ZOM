# Binder Conformance Runner

Binder conformance will consume `conformance/corpus` sources and
`conformance/expectations/binder` oracle files. No CTest target is registered
until the runner has executable semantics.
