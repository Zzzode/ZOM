section: "05-statements return diagnostics"
covers_rule: "ifStatement, returnStatement"
expected: "ACCEPT"
notes:
  - "Syntax is valid; diagnostics runner checks borrow-checker rejection for returning a reference to a local binding declared inside an else branch."
