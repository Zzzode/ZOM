section: "05-statements borrow diagnostics"
covers_rule: "ifStatement"
expected: "ACCEPT"
notes:
  - "Syntax is valid; diagnostics runner checks that a borrow scoped to an if branch no longer conflicts with a later mutable borrow."
