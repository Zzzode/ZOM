section: "04-expressions comparison trait conformance"
covers_rule: "standaloneImplDeclaration, binaryExpression"
expected: "ACCEPT"
notes:
  - "Syntax is valid; diagnostics runner checks that Ord.cmp(rhs: Point) -> i32 accepts <, <=, >, and >= through the CLI pipeline."
