// RUN: %zomc compile --dump-ast %s 2>&1 | %FileCheck %s

let [a, b] = [1, 2];
let {x, y} = {x: 10, y: 20};
let [first, ...rest] = [1, 2, 3];


// CHECK: Error [ZOM2077]: Declaration or statement expected. This follows a block of statements, so if you intended to write a destructuring assignment, you might need to wrap the whole assignment in parentheses
// CHECK:   --> {{.*destructuring.zom}}:4:12
// CHECK:   |
// CHECK: 4 | let {x, y} = {x: 10, y: 20};
// CHECK:   |            ^
// CHECK: {{.*zomc}} compile: Compilation failed due to parsing errors.
// CHECK: Try '{{.*zomc}} compile --help' for more information.
