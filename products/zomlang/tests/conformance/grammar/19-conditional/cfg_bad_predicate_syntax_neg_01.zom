// §19.3 Malformed cfg predicate: missing closing paren (syntax error)
// Covers rule: cfgPredicate (parenthesis mismatch → ANTLR native syntax error)
// Expected: REJECT
// ExpectedDiagnostic: line
#[zom::cfg(all(unix, target_os = "linux")]
fun bad_syntax() -> i32 { return 11; }
