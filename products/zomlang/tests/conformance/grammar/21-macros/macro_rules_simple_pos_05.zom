// §21.3 Declarative macro 2.0: simple `add!` with single (expr, expr) => { ... }
// Covers rule: macroRulesDecl, macroRule (paren alt), macroCapture ($a:expr / $b:expr)
// Expected: ACCEPT
// ExpectedDiagnostic: none
macro add! {
    ( $a:expr, $b:expr ) => {
        $a + $b
    }
}
