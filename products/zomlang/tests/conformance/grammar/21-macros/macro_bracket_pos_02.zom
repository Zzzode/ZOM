// §21.1 Function-like macro invocation with bracket delimiter `name![tt]`
// Covers rule: macroInvocationExpr (LBRACK alt)
// Expected: ACCEPT
// ExpectedDiagnostic: none
fun json_demo() -> unit {
    let arr = json_array![1, "hello", null, true];
    return arr;
}
