// §4.2 Interfaces
// Covers rule: interfaceMember (member without modifier prefix; modifierList allows 0 modifiers)
// Expected: ACCEPT

interface NoModifiers {
    fun bare() -> unit;
    get x() -> i32;
    set x(v: i32) -> unit;
    type T;
}
