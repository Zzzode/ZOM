// §3.7 Punctuators all compound
// Covers rule: HASH_LBRACK, COLON_COLON, QUESTION_COLON etc
// Expected: ACCEPT

#[attr]
fun f(a: i32) -> i32 {
    let x: std::io::Type = 0;
    let y = 1 ?: 2;
    let z = a ?? b ?? c;
    z++;
    z--;
    let w = !!a;
    let v = x?!;
}
