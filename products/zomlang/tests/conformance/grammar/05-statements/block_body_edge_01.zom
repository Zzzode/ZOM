// §4.5 Block with nested declarations and multiple statements
// Covers rule: blockBody, statementList
// Expected: ACCEPT

fun f() {
    let x = 1;
    {
        let y = 2;
        return x + y;
    }
}
