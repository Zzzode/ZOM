// §4.5 Label immediately followed by #[…] outer attribute (C25 #1 predicate)
// Covers rule: labeledStatement (checkLabelNoAttrAfterLabel)
// Expected: REJECT

fun f() {
    lab: #[inline] { x = 1; }
}
