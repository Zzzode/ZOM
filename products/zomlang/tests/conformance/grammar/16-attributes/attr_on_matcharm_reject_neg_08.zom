// §16 Attribute on Match Arm REJECT
// Covers rule: matchArm, no outerAttributeList prefix
// Expected: REJECT

fun f(x: i32) {
  match x {
    #[cfg::feature] 0 => { return 1; }
    _ => { return 2; }
  }
}
