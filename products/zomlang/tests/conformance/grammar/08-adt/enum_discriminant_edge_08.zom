// §8.3 Enum with unit variants only + explicit discriminant on some
// Covers rule: enumVariant
// Expected: ACCEPT

enum Weekday {
    Monday = 1,
    Tuesday,
    Wednesday,
    Thursday,
    Friday = 5,
    Saturday,
    Sunday,
}
