// §8.2 ClassProperty
// Covers rule: classProperty
// Expected: ACCEPT

class Person {
    private mut _name: str;
    public get name -> str { return self._name; }
    public set name(v: str) -> unit { self._name = v; }
}
