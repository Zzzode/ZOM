section: "05-statements borrow diagnostics"
covers_rule: "whileStatement"
expected: "ACCEPT"
notes:
  - "Syntax is valid; diagnostics runner checks that a borrow scoped to a while body no longer conflicts with a later mutable borrow."
