interface Drawable { }

class Good { }
class Box<T> { value: T; }

impl Drawable for Box<Good> { }

unsafe impl std::marker::Sendable for Box<Good>;

let value: Box<Good>;
let packed: dyn Drawable + Sendable = value;
